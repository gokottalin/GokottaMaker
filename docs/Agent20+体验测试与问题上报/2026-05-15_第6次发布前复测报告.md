# 2026-05-15 Agent20+ 第6次发布前复测报告

提交对象：Agent0
测试次数：第6次
测试日期：2026-05-15
测试类型：发布前复测
主测入口：`http://127.0.0.1:4335`
发布候选版本：`V2.4.9+20260514-1635`
Git Commit：`d6fc38c`

## 结论

本轮未发现 P0 / P1 发布阻断问题。

上一轮发布阻断项 `BUG-010` 已通过复测。Agent0 在 `2026-05-14_23-20_第4次复测结果处理与分工.md` 中列出的发布门槛，本轮逐项复测结果如下：

| 门槛项 | 本轮结果 | 证据 |
| --- | --- | --- |
| `390px` 首页无横向滚动 | 通过 | `390x844` 下 `/index.html` 的 `scrollWidth=clientWidth=390`，强制 `window.scrollTo(99999, 0)` 后 `scrollX=0` |
| `/admin/course-paths.html` 权限或本地工具定位明确 | 通过 | 未登录时 `#courseAdminShell.is-locked=true`，`#routeEditor.disabled=true`，保存/恢复/清空按钮均 `disabled=true` |
| `docs/agent-current-handoff.md` 与候选版本一致 | 通过 | 文档记录主测入口 `4335` 与版本 `V2.4.9+20260514-1635` |
| 分类页项目计数文案一致 | 通过 | `analog/stm32/esp32` 空搜索状态未再出现“4 个项目”旧文案 |
| `npm run test:markdown` 通过 | 通过 | `Markdown renderer regression tests passed.` |
| `scripts/verify-api.ps1` 通过 | 通过 | 隔离服务 `5497` 返回 `ok=True`，覆盖 login、csrf、upload、md2file、post/project CRUD、export、sitemap、rss、logout |
| `/healthz` 返回当前候选版本 | 通过 | `versionLabel=V2.4.9+20260514-1635`，`gitCommit=d6fc38c` |

## 测试覆盖

### 页面矩阵

使用 Chromium 自动化在 `1366x900`、`768x1024`、`390x844` 三组视口下检查以下页面：

- `/index.html`
- `/category.html?category=analog`
- `/category.html?category=stm32`
- `/category.html?category=esp32`
- `/category.html?category=all`
- `/projects.html`
- `/miniapps.html`
- `/tools/md2doc.html`
- `/tools/gokotta-elec.html`
- `/post.html?id=analog-active-filter`
- `/post.html?id=stm32-adc-dma-precision`
- `/project.html?id=logic-analyzer`
- `/project.html?id=smart-home-hub`
- `/admin/`
- `/admin/#health`
- `/admin/course-paths.html`

结果：

- 页面均可加载，无框架错误覆盖层。
- 页面级横向滚动均未复现。
- 首屏与滚动后的可见图片均可加载；自动化初扫中由 lazy-loading 造成的未加载图片，经滚动复核后不构成缺图问题。
- `/admin/course-paths.html` 未登录时会因主动请求 `/api/session` 在浏览器控制台产生一次 `401 Unauthorized` 网络日志；该请求对应未登录门禁判断，用户界面表现正确，本轮不作为发布阻断。

### 交互复测

| 功能 | 结果 | 证据 |
| --- | --- | --- |
| 首页“阅读全文” | 通过 | in-app browser 点击后进入文章页，文章标题与正文加载，控制台无 error/warn |
| 移动端导航菜单 | 通过 | `390px` 下点击 `[data-nav-toggle]` 后 `aria-expanded=true`、导航显示；按 `Escape` 后关闭 |
| 首页搜索 `STM32` | 通过 | 搜索后页面保留匹配内容，控制台无 error/warn |
| 分类页搜索 `ESP32` | 通过 | 搜索逻辑响应，控制台无 error/warn |
| MD2File 长文同步滚动与“对齐当前位置” | 通过 | 关闭同步滚动后，输入区滚动比 `0.68`，点击对齐后预览区滚动比 `0.68`，差值 `0` |
| LarkixElec Sample 生成 SVG | 通过 | Sample 数量 `5`，生成接口返回 `ok=true`，页面出现 SVG 预览图，IR 长度 `1377` |
| 文章目录 | 通过 | `/post.html?id=analog-active-filter` 正文 6 个 H2 章节，目录 6 个链接；“相关推荐”区标题未进入目录，符合文章目录范围 |

### API / 后端复测

直接针对 `4335` 验证：

- `/api/health`：`200`，`ok=true`，版本与 commit 正确。
- `/api/content`：`200`，公开文章 `4`，公开项目 `2`，项目目录 `4`。
- `/sitemap.xml`：`200`。
- `/rss.xml`：`200`。
- `/robots.txt`：`200`，包含 `Disallow: /admin/` 与 `Disallow: /api/`。
- `/api/elec/samples`：`200`，`samples=5`。
- `/api/elec/build`：`200`，`ok=true`，产物包含 `svg`、`ir`、`ercText`。
- `/api/md2file/convert` DOCX：`200`，返回 `release-check.docx`，`Content-Length=4881`。
- `/api/md2file/convert` PDF：`400`，返回 `FORMAT_UNSUPPORTED`，符合当前仅支持 DOCX 的预期。

## 已知历史问题复测状态

| 历史编号 | 状态 | 本轮说明 |
| --- | --- | --- |
| `BUG-010` | 已修复 | `360/375/390/414px` 移动端页面级横向滚动均为 `0`；本轮重点复测 `390px` 首页通过 |
| `AUTH-011` | 已修复 | 未登录课程路径管理页锁定，编辑器与保存相关按钮均 disabled，未登录实际 fill/click 被阻止，localStorage 未写入 |
| `ENV-012` | 已处理 | 交接文档版本、主测入口与发布候选一致 |
| `COPY-013` | 已修复 | 分类页空状态未再出现旧的“4 个项目”错误计数文案 |

## 测试限制

### LIMIT-014：当前 `4335` 候选服务缺少可用于 Agent20 的真实管理员密码

现象：

- `POST /api/login` 使用 README 默认凭据 `Larkix / change-this-before-public-deploy` 返回 `401`。
- 因此 Agent20 本轮无法在当前真实候选服务 `4335` 上复测“登录后的 CMS UI 写入流程”。

已覆盖的替代验证：

- `scripts/verify-api.ps1` 使用隔离 `DATA_DIR` 与一次性默认测试凭据自启 `5497` 服务，已完整通过 login、csrf、post/project CRUD、export、logout。
- 当前真实候选服务 `4335` 已验证未登录门禁、默认凭据不可登录、受保护接口未登录返回 `401`。

Agent0 判定建议：

- 若发布前必须验证真实候选数据库上的 CMS 登录态 UI，请通过安全渠道提供当前 `ADMIN_PASSWORD`，或由掌握凭据的 Agent / 管理者执行一次登录态 UI smoke。
- 若以后端隔离回归覆盖 CMS 写入即可，本项不阻断发布。

### NOTE-015：本机仍存在 `5040` 端口监听，但不是当前 Node 候选服务

现象：

- 当前 `4300-5500` 范围内监听端口为 `4335/node` 与 `5040/svchost`。
- `5040` 无 `/healthz` 响应，不是 LarkixMaker 当前候选服务。

Agent0 判定建议：

- 本轮发布验收仅以 `4335` 为主测入口。
- 如 Agent1 的端口清理口径要求“仅保留项目相关端口”，`5040/svchost` 可忽略；如要求本机端口完全无额外监听，需要 Agent1 单独确认其系统来源。

## 命令结果

通过：

```powershell
npm.cmd run check:version
npm.cmd run test:markdown
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-api.ps1
git diff --check
node --check main.js
node --check category-page.js
node --check post.js
node --check tools\md2doc.js
node --check tools\gokotta-elec.js
node --check data\footer.js
node --check data\markdown-renderer.js
node --check lib\md2doc.js
node --experimental-sqlite --check server.js
```

注意：

- 在 PowerShell 中直接执行 `npm run ...` 会被系统执行策略拦截 `npm.ps1`；本轮使用 `npm.cmd run ...` 执行，结果通过。
- `scripts/verify-api.ps1` 默认自启隔离服务 `http://127.0.0.1:5497`，不是对 `4335` 的原地写入测试。
