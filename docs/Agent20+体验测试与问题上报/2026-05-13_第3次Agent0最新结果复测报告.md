# Agent20+ 第3次 Agent0 最新结果复测报告

- 测试日期：2026-05-13
- 测试次数：第3次
- 测试类型：Agent0 最新整合结果复测
- 测试对象：GokottaMaker `V2.4.9+20260513-1148`
- 主要测试地址：`http://127.0.0.1:4402`
- 临时数据复测地址：`http://127.0.0.1:4413`、`http://127.0.0.1:4414`
- 上报对象：Agent0
- 处理原则：只上报问题，不修改业务代码。

## 复测结论

当前候选版本未发现 P0 阻断问题。第2次报告中的长文章目录和 MD2File 长文对齐问题已通过复测。

仍需 Agent0 处理 2 个测试交接类问题：

1. Agent0/Agent20 测试入口文档中的示例文章 ID 失效。
2. 当前 `4402` 候选服务未提供可用管理端测试凭据，导致 Agent20 无法在该真实数据服务上直接复测管理端写入类流程。

## 已通过的回归项

### 第2次报告问题复测

| 原编号 | 复测结论 | 证据 |
| --- | --- | --- |
| BUG-005 长文章左侧目录不能内部滚动 | 通过 | 长文目录 60 个链接，`.toc-card overflow-y=auto`，`max-height=644px`，`scrollTop` 可从 `0` 到 `1703` |
| BUG-006 文章目录不表达标题层级 | 通过 | 目录项已包含 `toc-level-2/3/4` 和 `data-level=2/3/4`，缩进分别为 `0px/16px/30px` |
| REQ-001 MD2File 缺少同步/对齐功能 | 通过 | 同步开：左 `0.62` / 右 `0.62`；同步关：左 `0.8199` / 右 `0`；点击对齐后右侧到 `0.82` |

### Agent0 准备清单重点项

| 项目 | 复测结论 |
| --- | --- |
| `/healthz` 版本 | 通过，`V2.4.9+20260513-1148` |
| 首页、分类页、项目页、小程序页、工具页 | 通过，HTTP 200，无 `console.error` / `pageerror` |
| 首页 1920 / 1279 / 390 视口 | 通过，无横向溢出，头部控件可见，Hero 卡片数为 3，未超过 4 |
| 小程序版本 | 通过，`MD2File V0.3`、`GokottaElec V1.3` 可见，无旧版本号 |
| 小程序和工具页夜间模式 | 通过，切换后 `htmlTheme=dark`，工具页面板进入深色态 |
| 管理端 UI 临时数据复测 | 通过，登录、密码显示、4 个轮播槽位、排序 0-3、封面选择按钮、Preview 收起扩大均可用 |
| API 回归脚本 | 通过，覆盖登录、CSRF、轮播槽位、上传、MD2File、CRUD、导出、sitemap、RSS、退出 |

已执行命令：

```powershell
node scripts\check-version.js
npm.cmd run test:markdown
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-api.ps1
node --check post.js
node --check tools\md2doc.js
node --check tools\gokotta-elec.js
node --experimental-sqlite --check server.js
node --check admin\admin.js
git diff --check
```

## ISSUE-008：Agent0 测试入口文档引用了不存在的示例文章 ID

- 严重级别：P2
- 类型：测试交接问题 / 复测入口错误
- 建议归属 Agent：Agent0
- 相关文件：
  - `docs/Agent20+体验测试与问题上报/2026-05-13_V2.4.9测试准备清单.md:18`
  - `docs/agent-current-handoff.md:105`

### 复现步骤

1. 启动或访问当前候选服务 `http://127.0.0.1:4402`。
2. 按 Agent0/Agent20 测试入口打开：`/post.html?id=active-low-pass-filter`。
3. 观察文章详情页正文和目录。

### 实际结果

页面返回 HTTP 200，但文章详情页显示空态：

```text
文章目录
没有找到内容。
```

运行时数据：

- 页面标题：`文章详情 | GokottaMaker`
- `hasEmptyState=true`
- `articleHeadingCount=0`
- `tocLinks=0`

当前 `/api/content` 中公开文章 ID 为：

```text
opensource-power-amplifier
analog-active-filter
stm32-adc-dma-precision
esp32-low-power-node
```

其中有效的模拟电子文章是：`/post.html?id=analog-active-filter`。

### 期望结果

Agent0 交接文档和 Agent20 测试准备清单中的示例文章入口必须指向当前候选数据中实际存在的公开文章。

### Agent0 处理要求

请将测试入口中的：

```text
/post.html?id=active-low-pass-filter
```

改为：

```text
/post.html?id=analog-active-filter
```

或在种子数据 / 路由兼容层中提供 `active-low-pass-filter` 的有效公开文章映射。两种方案选择其一即可，但文档入口和实际可访问内容必须一致。

## ENV-009：当前 `4402` 候选服务缺少可用管理端测试凭据说明

- 严重级别：P2
- 类型：测试环境阻塞 / 交接信息缺失
- 建议归属 Agent：Agent0
- 相关文件：
  - `docs/Agent20+体验测试与问题上报/2026-05-13_V2.4.9测试准备清单.md`
  - `docs/agent-current-handoff.md`

### 复现步骤

1. 打开当前候选服务：`http://127.0.0.1:4402/admin/index.html`。
2. 使用项目默认测试凭据登录：

```text
username = Gokotta
password = change-this-before-public-deploy
```

3. 观察 `/api/login` 返回结果。

### 实际结果

当前 `4402` 服务返回：

```text
HTTP 401
```

因此 Agent20 无法在 `4402` 真实候选数据服务上继续做管理端写入类复测，例如封面上传后访客端展示、保存内容进入数据库、轮播槽位替换等。

本轮为避免污染真实数据库，已改用临时数据目录启动同版本服务复测管理端 UI。临时服务中管理端 UI 行为通过：

- Dashboard 可进入
- 密码显示/隐藏可用
- 轮播槽位数量为 4
- 轮播排序选项仅 `0,1,2,3`
- 封面选择按钮可见
- 编辑器收起后 Preview 从 `758.922px` 扩大到 `1302px`

### 期望结果

Agent0 在交接给 Agent20 复测时，应明确提供以下任一方案：

1. 当前候选服务 `4402` 的可用管理端测试账号和密码。
2. 明确要求 Agent20 使用临时数据目录启动测试服务，并给出启动命令。
3. 提供已经初始化好测试账号的独立端口。

### Agent0 处理要求

请在 `docs/agent-current-handoff.md` 和 Agent20 测试准备清单中补充“管理端复测凭据 / 临时测试服务启动方式”。否则后续 Agent20 无法稳定复测管理端写入类功能，只能退回到临时数据服务验证 UI 行为。

## 发布判定

本轮未发现 P0 问题。若只看应用功能，当前候选版本可以继续进入 Agent1 云端发布前准备；但 Agent0 需要先修正测试入口文档，并补充管理端复测凭据说明，避免下一轮复测继续卡在同一个交接问题上。
