# 2026-05-14 Agent20+ 第5次移动端横向滚动复测报告

提交对象：Agent0
测试次数：第5次
测试日期：2026-05-14
测试范围：游客端移动端页面级横向滚动复测
测试结论：通过，本轮未发现页面级横向滚动。

## 测试环境

- 服务地址：`http://127.0.0.1:4335`
- 版本：`V2.4.9+20260514-1635`
- Git Commit：`b9303d3`
- 服务健康状态：`/healthz` 返回 `ok: true`
- 浏览器验证方式：Chromium 移动视口自动化复测，并用 in-app browser 打开 `http://127.0.0.1:4335/index.html` 确认页面可访问。

## 复测对象

- 复测上一轮上报的 `BUG-010`：`390px` 移动端首页课程路径区产生页面级横向溢出。
- 本轮仅验证“移动端无页面级横向滚动”；未重新验证 `AUTH-011`、`ENV-012`、`COPY-013`。

## 判定标准

页面判定为通过需要同时满足：

- `Math.max(document.documentElement.scrollWidth, document.body.scrollWidth) <= document.documentElement.clientWidth + 1`
- 执行 `window.scrollTo(99999, 0)` 后，`window.scrollX === 0`
- `document.documentElement.scrollLeft === 0`
- `document.body.scrollLeft === 0`

## 覆盖页面

本轮在 `360px`、`375px`、`390px`、`414px` 四组移动端宽度下复测以下游客端页面：

- `/index.html`
- `/category.html?category=analog`
- `/category.html?category=stm32`
- `/category.html?category=esp32`
- `/category.html?category=all`
- `/miniapps.html`
- `/projects.html`
- `/post.html?id=analog-active-filter`

## 关键结果

| 视口宽度 | 覆盖页面数 | 页面级最大 `scrollWidth - clientWidth` | 强制横向滚动后最大 `scrollX` | 结论 |
| --- | ---: | ---: | ---: | --- |
| `360px` | 8 | `0` | `0` | 通过 |
| `375px` | 8 | `0` | `0` | 通过 |
| `390px` | 8 | `0` | `0` | 通过 |
| `414px` | 8 | `0` | `0` | 通过 |

## 首页课程路径复测结果

`BUG-010` 原始触发视口为 `390px` 移动端首页。当前复测结果如下：

```json
{
  "viewport": "390x844",
  "path": "/index.html",
  "clientWidth": 390,
  "docScrollWidth": 390,
  "bodyScrollWidth": 390,
  "maxScrollWidth": 390,
  "scrollXAfterForceScroll": 0,
  "overflow": false
}
```

结论：`BUG-010` 可标记为已修复并通过 Agent20+ 第5次复测。

## 备注

- 文章页 `/post.html?id=analog-active-filter` 中代码片段内部存在超过可视宽度的 inline `code` 内容，但页面级 `scrollWidth` 未超过 `clientWidth`，强制横向滚动后 `scrollX` 仍为 `0`。该现象未形成页面级横向滚动。
- 本轮未发现新的移动端页面级横向滚动问题。
