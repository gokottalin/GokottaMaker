# Agent0 总控交接：第2次测试问题处理与后续分工

更新时间：2026-05-13 01:22
当前候选版本：V2.4.9+20260513-1148
输入报告：`docs/Agent20+体验测试与问题上报/2026-05-12_第2次测试问题报告.md`

## 当前状态

本轮已处理 Agent20 第2次测试报告中总控可直接处理的 3 个核心问题：

| 编号 | 优先级 | 状态 | 处理结果 |
|---|---|---|---|
| BUG-005 | P1 | 已修复 | 文章详情页 TOC 增加内部滚动、最大高度和滚动边界控制 |
| BUG-006 | P2 | 已修复 | TOC 保留 H2/H3/H4 层级并增加视觉缩进 |
| REQ-001 | P2 | 已增强 | MD2File 增加编辑/预览双向同步滚动与手动对齐按钮 |

同时完成：

- 站点版本更新到 `V2.4.9+20260513-1148`。
- 关键 JS 文件语法检查通过。
- DOCX 转换 smoke test 通过。
- 替换字符扫描通过，无 `U+FFFD` 污染。

## 本轮变更边界

主要文件：

- `post.js`
- `styles/20-content.css`
- `tools/md2doc.html`
- `tools/md2doc.js`
- `styles/md2doc.css`
- `data/markdown-renderer.js`
- `lib/md2doc.js`
- `server.js`
- 站点入口 HTML 与资源版本参数文件

注意：

- `data/markdown-renderer.js` 中存在 Agent9 之前的大量 Markdown/公式解析改动，本轮仅做兼容校验和编码污染清理，不应由其他 Agent 随意回退。
- `tools/md2doc.js` 当前同步滚动是比例同步，不是基于标题 AST 的语义同步。后续如要更精准，应由 Agent6/Leo 和 Agent9 协作。
- 本轮没有发布云端，发布权仍交给 Agent1。

## 验证记录

已通过：

```text
node scripts\check-version.js
node --check post.js
node --check tools\md2doc.js
node --check tools\gokotta-elec.js
node --check data\markdown-renderer.js
node --check lib\md2doc.js
node --check server.js
node -e "DOCX conversion smoke test"
replacement-character scan
```

本地冒烟：

- `/healthz` 200
- `post.html?id=agent20-long-toc-test` 200
- `tools/md2doc.html` 200

## Agent 分工与输入输出

### Agent20：体验测试与问题上报

执行顺序：第 1 位
输入：

- 本地候选版本 `V2.4.9+20260513-1148`
- 页面：`post.html?id=agent20-long-toc-test`
- 页面：`tools/md2doc.html`

任务：

- 复测长文章 TOC 是否可内部滚动。
- 复测 H2/H3/H4 层级是否可读。
- 复测 MD2File 长文编辑/预览同步是否可用。
- 覆盖 390px、1279px、1920px 宽度。

输出：

- 新测试报告，写入 `docs/Agent20+体验测试与问题上报/`。
- 每个问题必须包含 URL、复现步骤、期望结果、实际结果、优先级。

### Agent9：Markdown 解析器

执行顺序：第 2 位
输入：

- `data/markdown-renderer.js`
- `post.js`
- `tools/md2doc.js`
- Agent20 的 TOC 与 Markdown 复测结果

任务：

- 增加 Markdown 公式、上下标、根号、希腊字母、代码块、标题层级的解析测试。
- 确认长文章标题 AST 输出稳定，避免 TOC 缺级或 id 冲突。

输出：

- 测试脚本或测试说明。
- 如修改解析器，必须说明兼容影响。

### Agent4：访客端体验、视觉与前端架构

执行顺序：第 3 位
输入：

- `styles/20-content.css`
- `post.js`
- `post.html`
- `category.html`
- `projects.html`

任务：

- 检查文章详情页目录在明暗模式、桌面端、移动端的视觉表现。
- 调整 TOC 层级样式时不得破坏文章正文 Markdown 样式。
- 检查访客端标题、Hero、Logo、日夜模式是否仍符合当前视觉方向。

输出：

- 视觉回归记录。
- 如有 CSS 修改，需列出影响范围和截图说明。

### Agent6/Leo：小程序与工具页

执行顺序：第 4 位
输入：

- `tools/md2doc.html`
- `tools/md2doc.js`
- `styles/md2doc.css`
- `lib/md2doc.js`
- `/api/md2file/convert`

任务：

- 复测 MD2File 的同步滚动、手动对齐、DOCX 转换。
- 检查工具页版本、入口卡片和错误提示是否统一。
- 后续如要语义级滚动同步，需要与 Agent9 对齐 heading AST。

输出：

- 工具页测试报告。
- 如修改小程序页面，必须同步 `data/miniapps.js` 的版本和描述。

### Agent3：管理端 CMS 与内容工作流

执行顺序：第 5 位
输入：

- `admin/index.html`
- `admin/admin.js`
- `admin/admin.css`
- 公共 Markdown 样式变更

任务：

- 确认 CMS 预览不被本轮 Markdown 样式影响。
- 检查封面上传、轮播 0-3 槽位、内容蓝图、左右编辑布局。
- 确认资源版本号刷新后管理端没有缓存旧资源。

输出：

- CMS 回归报告。
- 如修改 admin 样式，避免影响 `styles/20-content.css` 的访客文章详情页。

### Agent2：后端与数据模型

执行顺序：第 6 位
输入：

- `server.js`
- `lib/md2doc.js`
- `scripts/verify-api.ps1`
- `/api/md2file/convert`
- `/uploads/*`

任务：

- 复核 API 返回 UTF-8 正常。
- 复核上传图片公开访问链路。
- 复核 Markdown/DOCX 接口大小限制和错误返回。
- 确认轮播最多 4 个、排序只允许 0-3。

输出：

- API 回归结果。
- 若涉及数据库清理，必须先生成备份路径。

### Agent1：运维与发布稳定性

执行顺序：第 7 位，最后执行
输入：

- Git 当前候选提交
- `scripts/deploy-update.sh`
- 云端 `/healthz`
- 本地验证报告

任务：

- 上线前确认云端工作树干净。
- 先备份数据库和上传目录。
- 拉取最新提交并重启服务。
- 验证公网 `/healthz`、首页、文章详情、工具页、上传图片。

输出：

- 发布报告，包含发布前后版本、commit、备份目录、健康检查 JSON。

### Agent5：内容、SEO 与知识体系

执行顺序：第 8 位
输入：

- `/rss.xml`
- `/sitemap.xml`
- `/robots.txt`
- 内容数据与公开状态

任务：

- 确认 sitemap/rss 不包含草稿、回收站、未上线项目详情。
- 检查文章标题层级是否利于 TOC 与 SEO。
- 补充下一批内容规划。

输出：

- SEO 与内容健康报告。

### Agent7：美观设计

执行顺序：第 9 位
任务：

- 审查 TOC 层级、MD2File 控件和整体视觉是否统一。
- 只给设计建议或小范围 CSS 建议，不直接改后端/数据。

### Agent8：市场部

执行顺序：第 10 位
任务：

- 根据 V2.4.9 更新内容准备发布说明和对外文案。
- 不参与代码合并。

## 当前建议

下一步应先让 Agent20 回归测试 V2.4.9。如果 Agent20 通过，再由 Agent1 负责 Git 推送与云端发布。若 Agent20 仍发现 MD2File 同步体验不够精准，则交给 Agent6/Leo 与 Agent9 继续做语义级对齐。
