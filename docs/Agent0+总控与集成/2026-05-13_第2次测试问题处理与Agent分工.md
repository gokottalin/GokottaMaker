# 第 2 次测试问题处理与 Agent 分工

更新时间：2026-05-13 17:59
负责人：Agent0 总控与集成
候选版本：V2.4.9+20260513-1148

## 当前状态

本轮已整合 Agent1、Agent2、Agent3、Agent4、Agent6、Agent9 的最新输出，并把当前可测试状态收敛到 `docs/agent-current-handoff.md`。

已修复的交接问题：

- 交接文档出现终端乱码，已重写为干净 UTF-8。
- Agent 输出散落在多个目录，已整理成统一成果矩阵。
- Agent20 缺少明确测试入口，已新增测试准备清单。
- PowerShell 执行策略会拦截 API 回归脚本，已在交接中补充 Bypass 执行方式。
- Agent20 第 3 次复测发现示例文章 ID 失效，已从 `active-low-pass-filter` 修正为 `analog-active-filter`。
- Agent20 第 3 次复测发现管理端复测凭据缺失，已补充安全说明和一次性本地测试服务启动方式。

## 集成验证

已通过：

- 版本一致性检查：`V2.4.9+20260513-1148`
- Markdown 渲染回归测试
- 前端与后端主要 JS 文件语法检查
- `git diff --check`
- API 回归：登录、CSRF、轮播槽位、上传、MD2File、文章 CRUD、项目 CRUD、导出、sitemap、RSS、退出

API 回归摘要：

```text
ok: True
versionLabel: V2.4.9+20260513-1148
publicPosts: 4
publicProjects: 2
featuredSlots: 0,1,2,3
csrfBlocked: True
carouselOrderBlocked: True
md2fileChecks: success, empty blocked, format blocked, size blocked
verified: login, csrf, carousel slots, upload, md2file, post CRUD, project CRUD, export, sitemap, rss, logout
```

## Agent 分工对齐

### Agent1 运维与发布稳定性

输入：

- 当前候选版本 `V2.4.9+20260513-1148`
- `docs/agent-current-handoff.md`
- 本地验证结果

输出：

- 云端发布结果
- `/healthz` 版本回执
- 若失败，提供 deploy 日志、备份路径、回滚建议

边界：

- 不改 CMS 交互逻辑。
- 不改访客端视觉。

### Agent2 后端与数据模型

输入：

- `server.js`
- `scripts/verify-api.ps1`
- 上传、轮播、MD2File、CRUD 相关问题

输出：

- 稳定 API 行为
- 数据迁移或兼容说明
- 回归脚本补充

边界：

- 不负责页面美术。
- 不直接调整管理端布局，除非接口需要新增字段。

### Agent3 管理端 CMS 与内容工作流

输入：

- Agent20 对 `/admin/index.html` 的批注
- 内容蓝图、编辑器、内容列表、图片库、轮播管理相关需求

输出：

- 管理端可用性修复
- 编辑器/Preview 交互改进
- 内容保存、上传、轮播槽位管理体验

边界：

- 不负责访客端整体视觉风格。
- 不改变后端数据结构，除非先和 Agent2 对齐。

### Agent4 访客端体验、视觉与前端架构

输入：

- Agent20 对首页、分类页、项目页、小程序页的批注
- 站点视觉规范、Logo、夜间模式、Hero 卡片问题

输出：

- 访客端页面视觉与交互修复
- 响应式布局验证
- 夜间模式一致性

边界：

- 不负责 CMS 保存逻辑。
- 不改后端接口契约。

### Agent5 内容、SEO 与知识体系

输入：

- 内容分类、文章结构、项目知识体系、SEO 需求

输出：

- 文章与项目内容框架
- RSS/sitemap/robots 内容建议
- 真实教程和项目文档补齐计划

边界：

- 不负责接口修复。
- 不负责复杂前端布局。

### Agent6 小程序

输入：

- `/miniapps.html`
- `/tools/gokotta-elec.html`
- `/tools/md2doc.html`
- 小程序版本与功能反馈

输出：

- 小程序功能修复
- 小程序卡片版本同步
- 独立工具页回归报告

边界：

- 小程序必须保持独立页面、独立样式和独立 API 契约。
- 不影响主站 CMS 与文章系统。

### Agent7 美观设计

输入：

- 登录页角色、品牌动效、视觉统一性问题

输出：

- 矢量角色、视觉规范、设计 token 建议
- 与 Agent4 合并时需要给出具体 CSS/资产边界

边界：

- 不直接改后端。
- 不直接改变 CMS 数据字段。

### Agent8 市场部

输入：

- 网站定位、首页文案、项目叙事、外部传播需求

输出：

- 品牌表达和内容营销建议
- 首页栏目文案优化

边界：

- 不负责工程实现。

### Agent9 Markdown 解析器

输入：

- Markdown 渲染问题、公式、代码块、目录、长标题

输出：

- 渲染器修复
- `npm run test:markdown` 回归用例

边界：

- 不负责页面布局。
- 不负责文章内容创作。

### Agent20 体验测试与问题上报

输入：

- 当前候选版本
- `docs/Agent20+体验测试与问题上报/2026-05-13_V2.4.9测试准备清单.md`

输出：

- 按页面、问题、严重级别整理的新测试报告
- 截图或批注
- 是否允许进入云端发布的结论

边界：

- 只做体验测试和问题上报。
- 不直接改代码。

## 后续执行顺序

1. Agent20 先测本地候选版本。
2. Agent0 汇总 Agent20 新问题。
3. P0 问题按归属派给 Agent2、Agent3、Agent4、Agent6、Agent9。
4. Agent0 合并后再跑完整验证。
5. Agent1 发布云端。

## 给 Agent20 的测试判定

可以开始测试，但需要重点盯以下区域：

- 云端上传图片是否显示，尤其是用户自己上传的图。
- 首页 Hero 卡片是否仍然过糊。
- 日间/夜间模式是否全站同步。
- CMS 内容蓝图在 1279px、1920px、移动端是否对齐。
- 编辑器收起后 Preview 是否真的展开。
- 小程序版本号是否准确。
