# 第4次复测结果处理与 Agent 分工

时间：2026-05-14 23:20
负责人：Agent0
来源：`docs/Agent20+体验测试与问题上报/2026-05-14_第4次复测报告.md`
主测版本：`V2.4.9+20260514-1635`
主测入口：`http://127.0.0.1:4335`

## 处理结论

本轮不允许直接发布。原因是 Agent20 发现 1 个 P0 阻断问题：390px 移动端首页课程路径区出现横向溢出，违反“移动端无横向滚动”的验收项。

Agent0 已直接处理交接文档问题：`docs/agent-current-handoff.md` 已更新为当前候选版本，并将管理端临时复测命令改为隔离 `DATA_DIR`，避免污染默认数据库。

## 问题分工

### BUG-010：390px 首页课程路径区横向溢出

优先级：P0
归属：Agent4
协作：Agent7 / Agent20
页面：`/index.html`
相关文件：

- `styles/20-content.css`
- `styles/40-responsive.css`

处理要求：

- 在小屏断点中显式把 `.course-path-card` 改为单列。
- 检查 `.course-path-side`、`.course-path-copy`、`.course-steps`、`.section-title-line.course-path-title-line` 不再撑出卡片。
- 不允许用页面级横向滚动解决。

最低修复方向：

```css
.course-path-card {
  grid-template-columns: 1fr;
}
```

验收标准：

```js
document.documentElement.scrollWidth <= document.documentElement.clientWidth + 2
```

必须输出：

- 390x844 首页截图
- `scrollWidth/clientWidth` 检查结果
- 说明是否影响桌面端三段式课程路径卡

### AUTH-011：`/admin/course-paths.html` 未登录可访问并写入本地覆盖配置

优先级：P1
归属：Agent3
协作：Agent2 / Agent4 / Agent0
页面：`/admin/course-paths.html`

Agent0 产品定位：

当前页面不能作为正式公网 CMS 功能直接发布。短期定位为“本地课程路径预览工具”，不应让未登录访客误以为可写入数据库。

分工：

- Agent3：决定管理端工作流方案，未登录时必须阻止编辑保存，或跳转/提示登录。
- Agent2：如果该功能升级为正式 CMS，需要设计受保护 API 与服务器持久化，不允许继续只写 `localStorage`。
- Agent4：如果短期保留为本地工具，需要修改页面文案，明确“仅当前浏览器本地预览，不会写入 CMS / 数据库”。
- Agent0：合并前确认该页面不会造成公网误导。

验收标准：

- 未登录访问 `/admin/course-paths.html` 不应显示可写入的正式管理界面。
- 未登录点击保存不应造成访客端被管理文案误导。
- 若仍写 `localStorage`，页面必须清楚标注“本地预览”。

### ENV-012：交接文档版本和临时测试命令过期

优先级：P1
归属：Agent0
状态：已处理
文件：`docs/agent-current-handoff.md`

已完成：

- 候选版本改为 `V2.4.9+20260514-1635`。
- 主测入口改为 `http://127.0.0.1:4335`。
- 管理端临时复测命令增加：

```powershell
$env:DATA_DIR='.tmp\agent20-admin-retest-data'
```

后续交给 Agent1：

- 发布前停止旧候选服务或明确标注不参与验收。
- 重点关注 `4402` 旧 build 和 `5040` 异常监听端口。

### COPY-013：分类页空状态提示项目数量与右栏数量不一致

优先级：P3
归属：Agent4
页面：

- `/category.html?category=analog`
- `/category.html?category=stm32`
- `/category.html?category=esp32`

处理要求：

- 文案计数必须与右栏实际展示数量一致。
- 当前右栏最多展示 3 个项目，则空状态不能说“还有 4 个项目”。

建议实现：

```js
const visibleProjectCount = Math.min(routeProjects.length, 3);
```

推荐文案：

```text
右侧已展示 3 个相关开源项目，更多项目可进入开源项目页查看。
```

## Agent 顺序

1. Agent4 先修复 BUG-010。该项未通过前不进入发布。
2. Agent20 对 390px 首页重新复测。
3. Agent3 / Agent4 处理 AUTH-011 的短期管理页定位。
4. Agent4 顺手修复 COPY-013。
5. Agent1 清理旧测试端口并准备发布。
6. Agent20 做最终发布前复测。

## 发布门槛

发布前必须同时满足：

- 390px 首页无横向滚动。
- `/admin/course-paths.html` 权限或本地工具定位明确。
- `docs/agent-current-handoff.md` 与实际候选版本一致。
- 分类页项目计数文案一致。
- `npm run test:markdown` 通过。
- `scripts/verify-api.ps1` 通过。
- `/healthz` 返回当前候选版本。

## Agent0 备注

当前工作区存在大量 Agent4/Agent7/Agent20 生成内容，Agent0 本次只处理交接文档和任务分配，不回退、不清理、不覆盖其他 Agent 的工作成果。
