# Agent4 课程布局方案 1 执行记录

- 日期：2026-05-13
- 对接分工：`docs/Agent0+总控与集成/2026-05-13_22-31_Agent7课程布局方案1分工.md`
- 课程内容输入：`docs/Agent5+内容、SEO与知识体系/2026-05-13_22-55_Agent5_课程路径内容与资源区规范.md`
- 视觉说明输入：`docs/Agent7+视觉品牌与样式设计/2026-05-13_课程布局方案1保留Hero对接说明.md`

## 本轮完成

1. 保留首页 Hero、搜索框、夜间模式、管理端入口、小程序入口与现有轮播逻辑。
2. 在 Hero 下方新增首页课程路径区 `course-path-section`。
3. 首页结构调整为：
   - Hero
   - Course Paths
   - Miniapp Tools
   - Recent Lessons
   - Open Projects
4. `category.html` 升级为三栏学习页：
   - 左侧 `syllabus-rail`
   - 中间 `lesson-main`
   - 右侧 `resource-panel`
5. `category=all` 切换为“全部教程 + 推荐学习路线”结构。
6. 移动端已清除首页横向溢出。
7. 根据后续批注补充首页课程路径左侧大图区域，并取消分类页课程大纲/资源区浮动定位。

## 新增/修改文件

- `data/course-meta.js`
- `index.html`
- `category.html`
- `main.js`
- `styles/20-content.css`
- `styles/40-responsive.css`

## 实现说明

### 首页

- 课程路径区使用 Agent5 提供的三条路线静态元数据：
  - 模拟电子
  - STM32
  - ESP32
- 每条路线包含：
  - 中文/英文标题
  - 路线摘要
  - 5 个阶段节点
  - 推荐内容
  - 进入路线 CTA
- “最新教程”改为“近期更新 / Recent Lessons”，并改为：
  - 左侧重点课程
  - 右侧紧凑课程列表
- 课程路径卡新增左侧实拍配图区域：
  - 模拟电子使用 `analog-cover`
  - STM32 使用 `stm32-cover`
  - ESP32 使用 `esp32-cover`

### 分类页

- `analog / stm32 / esp32`：
  - 左栏展示课程阶段
  - 中栏展示推荐学习起点 + 文章列表
  - 右栏展示工具、关键词、相关项目、推荐阅读
- 根据批注，`syllabus-rail` 与 `resource-panel` 均改回正常文档流，不再使用 sticky 浮动侧栏。
- `all`：
  - 左栏改为方向筛选
  - 右栏改为推荐学习路线

## 验证

已通过：

```text
node --check main.js
node scripts/check-version.js
git diff --check -- index.html category.html main.js styles/20-content.css styles/40-responsive.css data/course-meta.js
```

Playwright 回归通过：

- `index.html` 1920 宽正常
- `index.html` 1279 宽正常
- `index.html` 390 宽无横向滚动
- `category.html?category=analog` 1279 宽正常
- `category.html?category=all` 390 宽无横向滚动
- 首页和分类页夜间模式下课程卡、资源面板、文字颜色可读

## 风险与后续

1. 当前课程阶段仍为静态信息展示，尚未接入真实阶段筛选。
2. 推荐内容依赖现有文章/项目 id，缺失项会静默跳过。
3. 若下一轮需要“阶段标签”“路线内进度”或“CMS 可编辑课程路径”，需要 Agent2 / Agent3 再介入。
