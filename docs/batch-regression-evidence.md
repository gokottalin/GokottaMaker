# 2026-07-30 需求批次回归证据

## 结论

`DISPATCH-20260730-001` 的 S30-S40A 已完成统一复测。运行器执行 37 项检查，结果为
`37 passed, 0 failed`；291 个工作区变化路径全部完成归类，暂存路径为 0。浏览器复验覆盖
访客端、CMS、MD2File、桌面、半宽、移动端与明暗主题，未发现横向溢出、关键控件裁切、
原始 LaTeX 泄露或控制台告警/错误。

本轮仅使用随机临时 `DATA_DIR`、临时管理凭据和本机隔离端口。隔离服务与数据目录已按
PID 和绝对路径清理；未读取或修改当前/生产数据，未执行云端写入、部署、Git 暂存、提交
或推送。

## 统一运行入口

```powershell
npm.cmd run test:batch-regression
```

运行时间：2026-08-07。总耗时约 46 秒。

运行器覆盖版本、Markdown、共享数学渲染、计算书、公式目录、文章公式、公式抽屉、公式
版本与发布、线性/分支推导图、旧公式迁移、封面坐标、阅读时间、聚焦模式、轮播、行内
公式、聚焦媒体、明暗主题、公式绑定、MD2File、CMS 工作流、两条隔离 API 验证路径、语法、
受保护哈希、需求映射、编码、空白、冲突标记、凭据扫描、变化路径归类与 Codex 契约。

## 需求映射

| 需求 | 已验收切片 | 主要自动化证据 | 浏览器或界面证据 | 结果 |
| --- | --- | --- | --- | --- |
| REQ-20260730-001 首页四槽轮播权威管理 | S31 | `test-hero-carousel-authority.js`、轮播缓冲回归 | CMS 与访客投影保持槽位和可见性规则 | 通过 |
| REQ-20260730-002 聚焦卡片封面完整填充 | S32 | 聚焦媒体、封面坐标回归 | 1280/800/360 三宽度，无溢出且 Hero 独立 | 通过 |
| REQ-20260730-003 全站 LaTeX 与定界符一致 | S30 | `test:math-rendering`、Markdown、行内公式回归 | 访客与 CMS 未显示原始 LaTeX | 通过 |
| REQ-20260730-004 公式图从左到右自适应布局 | S37 | `test-formula-map-flow-layout.js` | 1440 亮色、390 暗色；缩放、拖动、点击与公式节点正常 | 通过 |
| REQ-20260730-005 公式绑定紫色上角标 | S33 | `test-formula-binding-marker.js` | 标记位置、可见性和跳转语义通过 | 通过 |
| REQ-20260730-006 选中 LaTeX 创建并绑定公式卡 | S35 | `test-article-formula-selection-create.js` | 选区创建、绑定与反馈工作流通过 | 通过 |
| REQ-20260730-007 文章与公式悬浮反馈 | S38 | `test-cms-floating-feedback-publish-bar.js` | CMS 1440/760/390、明暗主题和公式抽屉均通过 | 通过 |
| REQ-20260730-008 推导工作流恢复与旧关系迁移 | S36 | 旧关系迁移、分支与线性图回归 | 迁移后关系和推导入口可用 | 通过 |
| REQ-20260730-009 右下角可折叠文章发布栏 | S38 | CMS 发布栏专用回归 | 390 宽发布栏 `x=7`、宽 360，无裁切或横向溢出 | 通过 |
| REQ-20260730-010 聚焦模式仅由页面排布控制 | S31 | 轮播权威、聚焦模式回归 | CMS 状态与访客显示规则一致 | 通过 |
| REQ-20260730-011 MD2File 唯一公开小程序 | S39 | `test-md2file-public-entry.js` | 聚焦开/关、三入口、三宽度共 18 个组合通过 | 通过 |
| REQ-20260730-012 MD2File 使用最新 Markdown/LaTeX 解析器 | S34、S40A | MD2File 语义、共享渲染、重定向 API 验证 | 工具页 800/390 正常加载与交互 | 通过 |

## 浏览器矩阵

- 访客端：`/`、`/maker.html`、`/miniapps.html`、`/tools/md2doc.html`，覆盖 1280、800、
  390 宽度；页面主内容可见、无横向溢出、无原始 LaTeX。
- MD2File：品牌首页、Maker 首页和小程序中心各保持一个公开卡片，统一进入
  `/tools/md2doc.html`；聚焦开/关矩阵覆盖 18 个组合。
- CMS：1440 暗色、760 亮色、390 暗色均无横向溢出；文章发布栏、公式抽屉入口和主题
  切换可见。390 宽发布栏完整落在客户端宽度内。
- 控制台：本轮访客与 CMS 代表场景的 `warn/error` 记录均为 0；各实现切片的专用浏览器
  证据继续作为细分行为依据。

## 保护边界

- S30-S40A 接受范围内文本均通过严格 UTF-8、BOM、换行、尾随空白与冲突标记检查。
- 接受范围内 JavaScript 均通过 `node --check`；受保护文件哈希保持一致。
- 12 项需求均存在有效需求包、实现切片映射和已验收交接文件。
- API 验证分别覆盖直接 PowerShell 输出与 Node.js 重定向捕获，共保留 58 个 API 断言。
- 临时测试目录删除前校验系统临时根目录和专用目录名前缀，避免触碰当前数据。
- `npm.cmd run codex:contract` 纳入统一运行器并通过。

## 保留门禁

1. 本证据证明本地隔离数据与代表性浏览器场景通过，不等同于生产数据迁移、生产物理清理
   或云端发布授权。
2. 发布前文件分组、版本、回滚与 Git 动作仍由 S41 单独审查；S40 未执行任何 Git 写操作。
