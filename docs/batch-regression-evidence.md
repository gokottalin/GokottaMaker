# 2026-07-28 需求批次回归证据

## 结论

`DISPATCH-20260728-001` 的 S18-S26 实现已完成批量复测。统一回归运行器共执行
23 项检查，结果为 `23 passed, 0 failed`；独立浏览器矩阵覆盖访客端、CMS、推导页、
桌面、半宽、移动端及明暗主题，未发现横向溢出、元素裁切或主题状态不可见。

本轮仅使用随机临时 `DATA_DIR`、临时管理口令和本机隔离端口。未读取或修改当前/生产
数据库，未执行旧公式物理清理，未进行云端、部署、Git 暂存、提交或推送。

## 统一运行入口

```powershell
node scripts/run-batch-regression-evidence.js
```

运行时间：2026-07-29。总耗时约 42.1 秒。

通过项目：

1. Markdown 渲染、计算书、公式目录、文章公式、公式抽屉。
2. 公式版本、三态发布、线性图兼容、分支 DAG、旧公式隔离迁移。
3. 封面坐标、阅读时间、聚焦模式、轮播缓冲、行内公式。
4. 聚焦内容图片、全站深色主题、隔离 API/CMS 边界。
5. JavaScript 语法、受保护文件哈希、UTF-8/BOM、空白与冲突标记、Codex 契约。

运行器失败时返回非零退出码；失败输出会截断并脱敏。临时目录删除前执行路径边界
校验，避免误删非测试数据。

## 需求映射

| 需求 | 已验收切片 | 主要命令证据 | 浏览器/界面证据 | 结果 |
| --- | --- | --- | --- | --- |
| REQ-20260728-001 可还原的 16:9 封面裁剪 | S22 | `npm.cmd run test:post-cover-coordinates` | S25 聚焦图片夹具复用已保存裁剪坐标，三种宽度均完整填充容器 | 通过 |
| REQ-20260728-002 复杂行内公式排版 | S24 | `node scripts/test-inline-math-layout.js` | 访客文章、CMS、推导页覆盖 1280/800/360；下标、上标、分数和根式无裁切或横向溢出 | 通过 |
| REQ-20260728-003 Markdown 推导正文与三态发布 | S18 | `npm.cmd run test:formula-publication` | S26 访客/CMS 状态夹具覆盖草稿、发布、归档及不可用状态 | 通过 |
| REQ-20260728-004 旧公式验证迁移与清理 | S21 | `npm.cmd run test:legacy-formula-migration` | 迁移、映射、重定向、恢复和清理证明仅在隔离数据中验证 | 条件通过；生产物理清理未授权 |
| REQ-20260728-005 多分支无环推导图 | S19 | `npm.cmd run test:branching-derivation-graph`、`npm.cmd run test:linear-derivation-graph` | S26 访客/CMS 夹具覆盖依赖、断链、工具栏和画布状态 | 通过 |
| REQ-20260728-006 作者输入建议阅读时间 | S23 | `npm.cmd run test:post-reading-minutes` | 深浅主题夹具与文章表现回归未出现布局溢出 | 通过 |
| REQ-20260728-007 聚焦内容图片自动填充 | S25 | `node scripts/test-focused-content-media.js` | 1280/800/360 下方形、竖图、横图、超宽图和失败态均通过；Hero 未被纳入规则 | 通过 |
| REQ-20260728-008 常驻可折叠公式抽屉 | S20 | `npm.cmd run test:formula-authoring-drawer` | CMS 深浅主题下抽屉可见，无裁切和横向溢出 | 通过 |
| REQ-20260728-009 全站夜间主题一致性 | S26 | `node scripts/test-full-site-dark-theme.js` | 访客端与 CMS 覆盖 1280/800/360；正文、状态、禁用控件、抽屉和弹窗颜色均生效 | 通过 |

## 浏览器矩阵

### 行内公式

- 访客文章暗色：1280、800、360。
- CMS 亮色 1280、暗色 360。
- 推导页暗色 800。
- 每个场景均渲染 20 个下标和 5 个上标，文档宽度等于客户端宽度，未发现零尺寸、
  越界或被裁切的公式元素。

### 聚焦内容图片

- 1280、800、360 三种宽度均无页面横向溢出。
- 非裁剪图片使用 `object-fit: cover`；保存过坐标的竖图由裁剪视口覆盖容器，比例不被
  拉伸；容器均为 `overflow: hidden`。
- 正常图片状态为 `ready`，故意损坏的夹具稳定进入 `failed`。
- Hero 不含 `data-focused-card-media`，保持原有独立行为。

### 明暗主题

- 访客暗色：1280、800、360。
- CMS 暗色：1280、360；CMS 亮色：800。
- 各场景均无横向溢出；成功、警告、错误、禁用控件、聚焦模式门禁、公式抽屉与裁剪
  弹窗保持可见，背景和文字颜色随主题切换。
- 专用浏览器测试脚本负责控制台和资源失败断言；人工矩阵用于补充几何尺寸和计算样式
  观察，未重复注册控制台事件监听器。

## 保护边界

- `styles/20-content.css`、`admin/admin.css`、`styles/10-hero.css`、
  `styles/26-inline-math.css`、`styles/27-focused-content-media.css` 哈希通过。
- 接受范围内的 JavaScript 文件均通过 `node --check`。
- 接受范围内文件通过 UTF-8、BOM、换行、尾随空白和冲突标记检查。
- `npm.cmd run codex:contract` 通过。
- 三个浏览器夹具进程在验收后按 PID 精确停止。

## 保留门禁

1. REQ-20260728-004 的当前/生产数据物理清理仍被阻止。必须先取得最新备份、独立恢复
   演练、最终清理报告摘要和受影响行清单，再由所有者明确授权。
2. 发布前 Git 文件分组、版本和回滚检查进入 S28；S28 只产出审查与暂存计划，不执行
   Git 暂存、提交、推送或部署。
