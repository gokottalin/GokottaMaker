# Tencent Cloud Git Snapshot Release Gate

Generated by: A00_ProjectDirector

## Decision

- Status: approved for one Git snapshot publish.
- Owner authorization: 2026-07-25, request to update the current project version in Git before a Tencent Cloud update.
- Repository: `https://github.com/gokottalin/GokottaMaker.git`
- Branch: `main`
- Baseline before publish: `300e426`
- Scope: stage the explicit project snapshot, commit it, and push `main`.
- Production deployment: not included. Tencent Cloud update remains a separate Owner action.

This one-time authorization overrides the general Git staging closure only for
the paths listed in `docs/codex-workline/git_staging_plan.md`. It does not open
database mutation, cloud data synchronization, migration execution, deployment,
rollback, or unrelated implementation.

## Release Checks

- `git fetch --prune origin`: passed; local and remote `main` both started at `300e426`.
- `npm.cmd run check:version`: passed.
- `npm.cmd run test:markdown`: passed.
- `npm.cmd run test:calculation-book`: passed.
- `npm.cmd run codex:check`: passed with 497 contract checks, 27 resource checks, and only known large-file warnings.
- `scripts/verify-api.ps1`: passed against an isolated temporary data directory.
- Secret-pattern scan: no Tencent Cloud, OpenAI, GitHub token, or private-key material found in the release candidates.
- `git diff --check`: passed.

## Backup And Rollback

- Git rollback anchor before publish: `300e426`.
- Runtime databases, uploads, environment files, and local backups are excluded from Git.
- If Tencent Cloud verification fails, deploy the previous commit `300e426` while preserving cloud runtime data.

## Release Readiness

Ready for the authorized Git commit and push. A14 remains the active development
workline after this snapshot; this decision does not mark A14 complete.
