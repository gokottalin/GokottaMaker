# Tencent Cloud Git Snapshot Staging Plan

Generated by: A00_ProjectDirector

## Included

- Root source and governance: `.gitignore`, `AGENTS.md`, `ACTIVE_AGENT_DISPATCH.json`, `PROJECT_MAINTENANCE.json`, `PROJECT_WINDOW.md`, `TOP_ARCHITECT_HANDOFF.json`, `derive.html`, `main.js`, `post.js`, `server.js`, `site-layout.js`, `package.json`.
- Project Codex configuration: `.codex/config.toml`, `.codex/larkix-governance.json`, `.codex/agents/`.
- Active task contracts: `agents/`.
- CMS and visitor implementation: `admin/`, `data/markdown-renderer.js`, `lib/content.js`, `lib/md2doc.js`, `lib/validators.js`, `styles/20-content.css`, `styles/30-accessibility-print.css`.
- Calculation-book masters and deterministic generated web artifacts: `content/calculation-books/`, `schemas/`, `tools/calculation-book/`.
- Data-model source files: `migrations/011_knowledge_nodes.js`, `migrations/012_knowledge_links.js`, `migrations/013_public_focus_mode_default.js`.
- Verification and governance scripts: `scripts/codex-governance.js`, `scripts/codex-handoff.js`, `scripts/test-calculation-book.js`, `scripts/test-markdown-renderer.js`, `scripts/verify-api.ps1`.
- Active project documentation: `docs/PROJECT_CHARTER.md`, `docs/calculation-book-authoring-guide.md`, `docs/codex-workline/`, and `docs/prompts/`.
- Contract-required Agent0 evidence only: `docs/Agent0+总控与集成/AGENT_UPDATE_INDEX.json`, `docs/Agent0+总控与集成/2026-07-09_00-00_Agent0_Task0收敛完成记录_Batch1暂停.md`, and `docs/Agent0+总控与集成/2026-07-09_12-42_Agent0_完整项目情况对接记录.md`.

## Excluded

- `.env` and all credentials or machine-specific environment configuration.
- `database/` and `runtime-data/`.
- `uploads/`, except the already tracked allowlisted placeholder.
- `.codex-logs/`, `*.log`, test temporary directories, screenshots created only as local evidence, and local backups.
- `node_modules/` and other dependency caches.
- Tencent Cloud deployment itself, cloud database mutation, content synchronization, migrations, rollback, tags, and GitHub pull-request creation.
- Untracked legacy pyramid-Agent reports under `docs/Agent*` except the three contract-required Agent0 evidence files listed above. They remain local historical evidence and are not runtime dependencies.

## Staging Rule

Use explicit path arguments only. Do not run `git add .` or `git add -A`.
