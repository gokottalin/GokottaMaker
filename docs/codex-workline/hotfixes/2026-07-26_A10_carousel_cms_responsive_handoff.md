# A10 Carousel CMS And Responsive Hotfix Handoff

Generated by: A10_CmsKnowledgeNodeWorkflow, completed by A00_ProjectDirector
Workspace: `E:/Project/2607-LarkixWeb`
Recorded at: `2026-07-26`

## status

complete

## scope_completed

- Consumed `2026-07-26_A11_carousel_visibility_handoff.md`.
- Added visitor visibility state, reason code, Chinese reason, and recommended
  action to every occupied CMS carousel slot.
- Added a high-contrast hidden state and emphasized cleanup button.
- Kept cleanup safe: it only sets `featured=false`, requires confirmation, and
  explicitly states that article/project content remains.
- Moved the editor to one column at `1180px` and below.
- Made navigation, filters, list actions, headings, and carousel controls wrap
  at medium and narrow widths.

## files_created_or_changed

- `admin/index.html`
- `admin/admin.js`
- `admin/admin.css`
- `docs/codex-workline/hotfixes/2026-07-26_A10_carousel_cms_responsive_handoff.md`

## decisions

- The CMS consumes `publicFocusMode` from `/api/admin/content`.
- Reason codes and rule order match the A11 handoff.
- Hidden carousel cleanup never deletes, archives, or changes publication state.
- `1180px` is the medium-width layout boundary so a half-width desktop browser
  no longer keeps the minimum `420px` editor beside the preview.

## risks

- Focus tokens are mirrored from `main.js`; both locations must change together
  until the rule is moved into a shared browser module.
- A00 still needs browser screenshots and overflow checks before release.

## tests_or_checks

- `node --check admin/admin.js`: passed.
- Release candidate: `V2.5.1+20260726-1650`.
- Current data, focus disabled: four occupied slots show `VISITOR_VISIBLE`.
- Isolated focus-enabled data: three occupied slots show
  `VISITOR_HIDDEN_FOCUS_SCOPE`, one shows `VISITOR_VISIBLE`, and hidden slots
  show `建议取消轮播`.
- Width checks at `1366`, `960`, `768`, and `390`: four slots remain reachable
  and `documentElement.scrollWidth <= clientWidth`.
- At `960px`, the editor grid resolves to block/single-column layout and the
  carousel resolves to two columns.
- At `390px`, the carousel resolves to one column; hidden status, long reason
  code, and action buttons do not create horizontal overflow.

## next_handoff

A00 must run syntax, contract, API, and visual regression checks, then update the
release version and publish the hotfix.
