# S03 API Runtime Boundary Handoff

Generated by: A08_ApiRuntimeBoundary
Workspace: `E:/Project/2607-LarkixWeb`
Recorded at: `2026-07-10`

## status

complete

## scope_completed

- Ran `npm.cmd run codex:handoff` and followed `agents/A08_ApiRuntimeBoundary/brief.md`.
- Read the active governance window, charter, task registry, implementation slices, S01 contract, S02 handoff, maintenance/dispatch/architect files, and the allowed source files.
- Added public knowledge-node API behavior:
  - `GET /api/knowledge-nodes` lists only `publishStatus='published'`, `visibilityStatus='public'`, non-deleted nodes.
  - `GET /api/knowledge-nodes/:slug` returns published non-deleted `public` or `unlisted` direct detail nodes.
  - Public API returns `404` for draft, archived, private, and soft-deleted nodes without exposing revisions or audit details.
- Added admin knowledge-node API behavior:
  - `GET /api/admin/knowledge-nodes`
  - `GET /api/admin/knowledge-nodes/:id`
  - `POST /api/admin/knowledge-nodes`
  - `DELETE /api/admin/knowledge-nodes/:id`
  - `POST /api/admin/knowledge-nodes/:id/restore`
  - `GET /api/admin/knowledge-nodes/:id/revisions`
  - `POST /api/admin/knowledge-nodes/:id/revisions/:revisionId/restore`
- Kept admin save response shape as `{ node, nodes, warnings }`.
- Added validation for node slug, node type, cover path, accent color token, publish completeness, visibility state, and derive-shortcode target/color/label.
- Added transaction-safe knowledge-node save, revision snapshot, soft-delete, restore, revision restore, raw Markdown derive-link extraction, `knowledge_links` replacement sync, dangling-link warnings, and audit writes.
- Extended isolated `DATA_DIR` API verification to cover knowledge-node validation, public filtering, admin save/update, revision restore, soft-delete/restore, direct unlisted detail, and safe dangling-link output.
- Improved isolated `.verify-api-data` cleanup with workspace path checking and retry to avoid Windows SQLite WAL/SHM release timing failures.

## files_created_or_changed

- `server.js`
- `lib/content.js`
- `lib/validators.js`
- `scripts/verify-api.ps1`
- `docs/codex-workline/slices/S03_api_runtime_boundary_handoff.md`

`lib/uploads.js` was read but did not require changes.

## decisions

- Kept derivation nodes out of `/api/content` and visitor/CMS UI files in this slice. Public derivation consumption should use the new `/api/knowledge-nodes` endpoints until S06 opens frontend work.
- Included `knowledgeNodes` in admin content/export responses because this is admin API/runtime scope, not CMS UI implementation.
- Allowed public direct detail for `visibilityStatus='unlisted'` per S01, while keeping unlisted nodes out of public lists.
- Implemented raw Markdown derive-shortcode extraction for knowledge-node saves only. Post/project link sync remains deferred because their save paths were not opened for this slice.
- Did not edit Markdown/DOCX rendering. S04 still owns rendered HTML/DOCX behavior for derive shortcodes.
- Audit metadata intentionally stores only compact fields: slug, publishStatus, visibilityStatus, accentColor, linksCount, danglingCount, symbol, and revisionId where applicable. It does not store full markdown, uploads, tokens, sessions, secrets, or environment/runtime paths.

## risks

- The CMS UI is not wired to these endpoints yet; S05 must consume and smoke-test the admin workflow.
- Public frontend pages are not wired to `/api/knowledge-nodes` yet; S06 must implement `derive.html?slug=<slug>` or its accepted equivalent.
- Markdown rendering and DOCX fallback for derive shortcodes are still S04 work. This slice only extracts and persists links from raw Markdown on knowledge-node saves.
- Public backlink/link summaries avoid target content for hidden targets, but visual click behavior and final public UX still depend on S04/S06.
- Production/current data migration and release remain closed. Only isolated `DATA_DIR` verification was run.

## tests_or_checks

- `npm.cmd run codex:handoff`: passed; routed to `A08_ApiRuntimeBoundary`.
- `npm.cmd run codex:contract`: passed before implementation, `361 passed, 0 warnings, 0 failures`.
- `node --check server.js`: passed.
- `node --check lib/content.js`: passed.
- `node --check lib/validators.js`: passed.
- PowerShell parse check for `scripts/verify-api.ps1`: passed.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-api.ps1`: passed with isolated `DATA_DIR=E:\Project\2607-LarkixWeb\.verify-api-data`; result included `publicKnowledgeNodes=1`, `csrfBlocked=True`, knowledge-node public/admin boundary coverage, and cleanup completed.
- `node --experimental-sqlite --check server.js`: passed.
- `npm.cmd run check:version`: passed, `V2.4.10+20260528-001`.
- `npm.cmd run test:markdown`: passed.
- `npm.cmd run codex:contract`: passed after implementation, `361 passed, 0 warnings, 0 failures`.

No CMS UI, visitor frontend, Markdown/DOCX renderer, current database/runtime data, production release, deployment, Git staging, commit, push, or rollback was performed.

## next_handoff

Return to A00 for acceptance and next gate decision. Suggested next prompt:

```text
Agent 00 Project Director（项目导演：验收 S03 API 运行边界并决定下一门禁）：请进入 E:\Project\2607-LarkixWeb，运行 npm.cmd run codex:handoff，读取 docs/codex-workline/slices/S03_api_runtime_boundary_handoff.md，验收 A08 输出；不得运行生产迁移、不得修改 CMS UI/访客前端/Markdown-DOCX、不得 staging/commit/push，除非治理文件明确打开下一步。
```
