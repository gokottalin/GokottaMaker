# S04 Markdown DOCX Derivation Links Handoff

Generated by: A09_MarkdownDocxDerivationLinks
Workspace: `E:/Project/2607-LarkixWeb`
Recorded at: `2026-07-10`

## status

complete

## scope_completed

- 运行 `npm.cmd run codex:handoff`，按 `agents/A09_MarkdownDocxDerivationLinks/brief.md` 执行。
- 读取 `AGENTS.md`、`PROJECT_WINDOW.md`、`docs/PROJECT_CHARTER.md`、任务注册表、实现切片、S01 合同、S03 handoff、Agent9/Agent7 短码与视觉 token 契约，以及本 slice 允许的 Markdown/DOCX 源文件。
- 在 Markdown inline 渲染中实现 `{{derive:slug|label|color}}` 与 `{{derive:slug|label}}`：
  - 输出 `derivation-link derivation-link--{token}`。
  - 输出 `href="./derive.html?slug=<slug>"`、`data-derive-slug`、`data-derive-label`、`data-derive-color` 和中文 `aria-label`。
  - `color` 仅允许 `purple|blue|green|amber|red|neutral`，未知 token 安全降级为 `purple`。
  - `label` 只作为转义文本输出，不解析 Markdown/HTML/嵌套短码。
  - 非法 slug、空/超长 label 不生成链接，保留为已转义文本。
- 短码解析放在 code/math token 保护之后，因此 fenced code、inline code、inline math、display math 中的 derive 短码不会生成链接。
- 在 DOCX 导出文本抽取中将 `.derivation-link` 降级为 `label [derive:slug]`，不引入 Word hyperlink relationship。
- 增加 Markdown/DOCX 回归，覆盖有效渲染、默认颜色、非法颜色降级、HTML label 转义、非法 slug/label 降级、code/math 排除、DOCX V0 可读 fallback。

## files_created_or_changed

- `data/markdown-renderer.js`
- `lib/md2doc.js`
- `scripts/test-markdown-renderer.js`
- `docs/codex-workline/slices/S04_markdown_docx_derivation_links_handoff.md`

`tools/md2doc.js` 已读取；本轮无需修改。

## decisions

- 采用 Agent9 推荐的 DOCX V0 方案 A：输出 `D.boost [derive:d-boost]`，保留离线目标信息。
- 网页渲染使用相对路由 `./derive.html?slug=<slug>`，与 S01/Agent9 V0 路由契约一致。
- 渲染层对未知 color token 降级为 `purple`，避免产生未批准 class；A08 保存/验证路径仍可继续拒绝非法 color，不改变数据库/API 入库语义。
- 没有输出 inline style，没有新增 CSS token 或 visitor frontend 样式；视觉落地仍归后续样式/前端 slice。
- 没有改动 A08 的 raw Markdown link sync helper，保持知识链接入库路径和当前 API/runtime 边界不变。

## risks

- `.derivation-link` 目前只有 HTML class/data 契约，专用 CSS 尚未实现；真实视觉状态需后续前端/样式 slice 承接。
- `derive.html?slug=<slug>` 的公开页面和点击后的 404/可见性 UX 尚未实现；S06 仍需消费 A08 API。
- DOCX V0 是纯文本 fallback，不是可点击 Word 超链接；若后续要求 hyperlink relationship，应另开 DOCX V1 slice。
- A08 的 raw Markdown 抽取/验证仍是独立实现；本轮未修改 `lib/content.js` 或 `lib/validators.js`。

## tests_or_checks

- `npm.cmd run codex:contract`: 变更前通过，`392 passed, 0 warnings, 0 failures`。
- `node --check data/markdown-renderer.js`: passed。
- `node --check lib/md2doc.js`: passed。
- `node --check scripts/test-markdown-renderer.js`: passed。
- `npm.cmd run test:markdown`: passed，输出 `Markdown renderer and MD2File DOCX regression tests passed.`。
- `git diff --check -- data/markdown-renderer.js lib/md2doc.js scripts/test-markdown-renderer.js`: exit code 0；Git 仅提示工作区行尾会按 CRLF 归一化。
- `npm.cmd run check:version`: passed，`V2.4.10+20260528-001`。
- `npm.cmd run codex:contract`: 变更后通过，`392 passed, 0 warnings, 0 failures`。

未执行生产迁移、当前/生产数据库写入、CMS UI/访客前端修改、发布、部署、回滚、Git staging、commit、push。

## next_handoff

返回 A00 验收 S04，并决定是否打开下一 slice。建议下一轮短提示：

```text
Agent 00 Project Director（项目导演：验收 S04 Markdown/DOCX 推导短码并决定下一门禁）：请进入 E:\Project\2607-LarkixWeb，运行 npm.cmd run codex:handoff，读取 docs/codex-workline/slices/S04_markdown_docx_derivation_links_handoff.md，验收 A09 输出；不得运行生产迁移、不得修改 CMS UI/访客前端/数据库/发布/Git，除非治理文件明确打开下一步。
```
