# S16 Focus Mode Scope Gate Handoff

Generated by: `A22_FocusModeScopeGate`
Workspace: `E:/Project/2607-LarkixWeb`
Recorded at: `2026-07-27`

## status

`complete`

业务实现、隔离数据验证、API/SEO/回归、桌面/移动端/半宽浏览器验收与契约检查均已完成，可交回 A00 接收 S16。A23 只能在 A00 完成接收和门禁更新后开放。

## scope_completed

- 建立唯一、服务端权威的 `public_focus_mode` 全局状态；新隔离数据默认开启，Owner 保存后的状态在服务重启后保持。
- 聚焦范围固定为 `electronics-basics`、`derivations`、`projects`；`power-electronics` 仅作为 `electronics-basics` 的兼容别名，不重命名或删除存量业务分类。
- 聚焦开启时，公共内容载荷、首页、导航、分类列表、搜索、RSS、sitemap 与文章/项目直接路由使用同一范围判定；越界详情返回 404。
- 聚焦开启时，CMS 列表、分类选择器与公式版本决策隐藏越界内容；服务端拒绝越界新建，并对已有越界内容的直接修改返回 404。
- CMS 只提供一个全局开关，显示当前状态、可见/存储数量及“关闭后原本已发布内容立即重新公开”的高影响提示。
- 关闭聚焦后，全部存量分类和原本已发布内容立即恢复；草稿只在 CMS 恢复且保持草稿。重新开启只改变可见性，不删除记录、不改发布状态。
- 聚焦首页入口顺序固定为电子基础、公式推导、开源项目，电子基础为第一主入口，不执行强制跳转。
- 未实现 A23 轮播缓冲、自动恢复或槽位重排；轮播仍读取原存储记录，等待下一独立门禁。

## files_created_or_changed

新增：

- `migrations/018_focus_mode_scope_gate.js`
- `scripts/test-focus-mode.js`
- `docs/codex-workline/slices/S16_focus_mode_scope_gate_handoff.md`

局部修改：

- `server.js`
- `lib/content.js`
- `lib/validators.js`
- `admin/index.html`
- `admin/admin.js`
- `admin/admin.css`
- `data/content-store.js`
- `main.js`
- `post.js`
- `category-page.js`
- `derive.html`
- `scripts/verify-api.ps1`
- `package.json`

未修改 A22 allowlist 之外的 SEO 模块、站点布局模块、既有 migration 013–017、当前/生产数据库、云端、部署或 Git 状态。

## scope_mapping

| 输入类型或标记 | 规范范围 | 说明 |
| --- | --- | --- |
| 文章 `categoryKey=electronics-basics` | `electronics-basics` | 直接规范分类 |
| 文章 `categoryKey=power-electronics` | `electronics-basics` | 兼容别名 |
| 文章分类为“电子基础”或“电力电子” | `electronics-basics` | 兼容现有中文分类 |
| 项目记录或项目文章 | `projects` | 开源项目范围 |
| 推导节点、`module:derivations`、公式标签或公式/推导短码文章 | `derivations` | 公式推导范围 |
| 其他模拟电子、STM32、ESP32、工程等内容 | 空范围 | 聚焦开启时隐藏并禁止写入 |

## reason_codes

| reason code | 场景 |
| --- | --- |
| `FOCUS_MODE_ENABLED` | 公共载荷表明聚焦已开启 |
| `FOCUS_MODE_DISABLED` | 公共载荷表明聚焦已关闭 |
| `FOCUS_SCOPE_ALLOWED` | 内容处于允许范围 |
| `FOCUS_SCOPE_OUTSIDE` | 聚焦开启时越界新建/访问或 CMS 决策越界 |
| `FOCUS_HIDDEN_OR_NOT_PUBLIC` | 公共详情不存在、未发布或被聚焦范围隐藏 |
| `FOCUS_MODE_ENABLED_BY_OWNER` | Owner 保存开启状态 |
| `FOCUS_MODE_DISABLED_BY_OWNER` | Owner 保存关闭状态 |

## state_transition_matrix

| 原状态 | 操作 | 公共端结果 | CMS 结果 | 数据状态 |
| --- | --- | --- | --- | --- |
| 新隔离环境 | 启动 | 默认聚焦，仅三类范围可见 | 默认聚焦，仅三类范围可编辑 | `ownerConfigured=false` |
| 开启 | Owner 关闭 | 原本已发布的所有分类立即恢复 | 所有内容和草稿恢复 | 只保存开关，状态不变 |
| 关闭 | 服务重启 | 仍为关闭 | 仍为关闭 | `ownerConfigured=true` |
| 关闭 | Owner 开启 | 越界已发布内容立即隐藏 | 越界内容和选择器隐藏 | 不删除、不归档、不改发布状态 |
| 开启 | 服务重启 | 仍为开启 | 仍为开启 | Owner 状态保持 |
| 开启 | 越界新建 | 不适用 | HTTP 409 `FOCUS_SCOPE_OUTSIDE` | 不写入 |
| 开启 | 越界已有记录直接修改 | 404 | HTTP 404 | 原记录不变 |

## public_and_cms_counts

隔离测试以 4 篇种子文章、4 个种子项目、1 个公开推导节点为基础，并新增 1 篇聚焦已发布文章、1 篇越界已发布文章、1 篇越界草稿：

| 状态 | 公共文章 | CMS 文章 | 公共项目 | CMS 项目 | 公共/CMS 推导节点 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 新环境默认开启 | 1 | 1 | 2 | 4 | 1 / 1 |
| 关闭并写入三条夹具后 | 6 | 7 | 2 | 4 | 1 / 1 |
| 重新开启 | 2 | 2 | 2 | 4 | 1 / 1 |

重新开启后的 CMS 计数同时报告文章 `visible=2`、`stored=7`，项目 `visible=4`、`stored=4`，证明隐藏不是删除。

## direct_route_evidence

- 越界已发布文章：
  - `GET /api/public/posts/focus-hidden-published` -> 404，`FOCUS_HIDDEN_OR_NOT_PUBLIC`
  - `GET /post.html?id=focus-hidden-published` -> 404
- 越界入口：
  - `GET /category.html?category=analog` -> 404
  - `GET /miniapps.html` -> 404
- 规范入口与兼容别名：
  - `GET /category.html?category=electronics-basics` -> 200
  - `GET /category.html?category=power-electronics` -> 200
- 聚焦 sitemap 包含电子基础、推导、项目和允许的详情；不含 `analog/stm32/esp32/all` 分类、miniapps 或越界夹具。
- RSS 不包含越界已发布夹具。

## restart_and_preservation_evidence

- Owner 关闭后重启：`enabled=false`、`ownerConfigured=true`。
- Owner 重新开启后重启：`enabled=true`、`ownerConfigured=true`。
- 关闭时创建的越界已发布文章重启与重新开启后仍为 `published`。
- 关闭时创建的越界草稿重启与重新开启后仍为 `draft`。
- 三条测试文章均存在且 `deleted_at IS NULL`。
- migration `018_focus_mode_scope_gate` 只执行一次，`PRAGMA integrity_check=ok`。
- 隔离服务、数据库和临时目录均已关闭并清理；未触碰当前或生产数据。

## decisions

- 服务端范围判定是权威边界，前端过滤只用于回退和即时 UI；不能依赖隐藏选项代替 API 拒绝。
- 已有越界记录的直接修改返回 404，避免通过 CMS/API 探测隐藏内容；越界新建返回 409 和稳定 reason code，便于明确修正分类。
- `power-electronics` 保持 URL/元数据兼容，不创建新的存储分类迁移。
- 项目记录天然属于 `projects`；文章需由规范分类、模块标签、公式语义标签或公式/推导短码明确归入范围。
- sitemap 的聚焦输出在 `server.js` 中实现，避免修改 A22 allowlist 之外的 `lib/seo.js`。
- 导出仍读取原始存储数据，确保本地备份不因可见性门禁丢失隐藏记录。

## risks

- 前端保留了服务端不可用时的本地回退判定；服务端正常时始终以 `/api/content.js` 的过滤结果为准。
- 当前只定义聚焦范围与可见性，不处理轮播槽位缓冲。A23 必须基于本交接另行实现幂等缓冲和 Owner 手动恢复。
- 聚焦关闭会按产品要求立即扩大公开范围，CMS 已提供确认提示，但生产启用前仍应由 Owner 审核内容状态。
- 聚焦首页将电子基础固定为首个主入口，并在其后保留当前符合范围的轮播内容；A23 仍需负责持久缓冲越界轮播项，不能把这一排序行为当作缓冲实现。

## tests_or_checks

已通过：

- `npm.cmd run test:focus-mode`
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-api.ps1`
- `npm.cmd run test:article-formula-authoring`
- `npm.cmd run test:formula-reference-versioning`
- `npm.cmd run test:linear-derivation-graph`
- `npm.cmd run test:formula-catalog`
- `npm.cmd run test:calculation-book`
- `npm.cmd run test:markdown`
- `node --check server.js admin/admin.js main.js category-page.js post.js data/content-store.js scripts/test-focus-mode.js`
- `git diff --check`
- `npm.cmd run codex:contract`：584 passed，0 warnings，0 failures

浏览器验收：

- 1280 × 720 游客首页：导航顺序为首页、电子基础、公式推导、开源项目；初始主入口标题为“电子基础学习路径”，链接为 `category=electronics-basics`；三个聚焦入口顺序正确；公开“进入管理端”按钮不可见。
- 430 × 900 游客首页：聚焦入口为单列，`scrollWidth=clientWidth=415`，无横向溢出。
- 720 × 900 半宽 CMS：唯一开关 `switchCount=1`，聚焦面板两列，无重叠，`scrollWidth=clientWidth=705`。
- 430 × 900 CMS：聚焦面板单列，`scrollWidth=clientWidth=415`，状态和高影响警告可读。
- 关闭后 CMS 状态为文章 `4/4`，模拟电子、STM32、ESP32、开源项目均恢复；从 UI 重新开启后立即回到文章 `1/4`，越界文章消失，项目保持 `4/4`。
- 游客首页与 CMS 页面控制台均无页面 `error`、`warn` 或 `warning`。
- 浏览器验收发现并修复了首页首屏未优先指向电子基础以及 `hidden` 属性被按钮样式覆盖的问题；修复后专项与全套回归重新通过。

未执行当前/生产数据写入、生产 migration、云同步、部署、Git staging、commit、push 或 rollback。

## next_handoff

立即按 `AGENTS.md` 自动交回 A00 验收 S16；A00 验收通过后更新 registry/gate，运行 `npm.cmd run codex:handoff`，并把 A23 brief 直接派发给 `A23_CarouselFocusBuffer` 职能 Agent，不等待 Owner 再发送“继续”。
