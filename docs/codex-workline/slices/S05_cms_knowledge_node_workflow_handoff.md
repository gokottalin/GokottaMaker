# S05 CMS Knowledge Node Workflow Handoff

Generated by: A10_CmsKnowledgeNodeWorkflow
Workspace: `E:/Project/2607-LarkixWeb`
Recorded at: `2026-07-10`

## status

complete

## scope_completed

- 运行 `npm.cmd run codex:handoff`，按 `agents/A10_CmsKnowledgeNodeWorkflow/brief.md` 执行。
- 读取 `AGENTS.md`、`PROJECT_WINDOW.md`、`docs/PROJECT_CHARTER.md`、任务注册表、实现切片、S03 API handoff、S04 Markdown/DOCX handoff，以及允许读取的 CMS 文件。
- 在 CMS 侧栏新增“推导节点”入口，并新增推导知识节点专用列表视图。
- 扩展现有内容编辑器，支持 `knowledge_node` 内容类型和 A08 V0 字段：`id`/`slug`、`nodeType`、`symbol`、`title`、`summary`、`markdown`、`cover`、`accentColor`、`tags`、`publishStatus`、`visibilityStatus`。
- 复用现有封面裁剪/上传、图片库、Markdown 文件导入和 `window.LarkixMarkdown.render()` 预览路径；A09 `{{derive:slug|label|color}}` 短码在管理端预览中走同一渲染器。
- 接入已有 A08 管理端 API：
  - `POST /api/admin/knowledge-nodes` 保存、新建、更新、发布、转草稿。
  - `DELETE /api/admin/knowledge-nodes/:id` 软删除。
  - `POST /api/admin/knowledge-nodes/:id/restore` 恢复。
  - `GET /api/admin/knowledge-nodes/:id` 和 `GET /api/admin/knowledge-nodes/:id/revisions` 加载详情与版本记录。
  - `POST /api/admin/knowledge-nodes/:id/revisions/:revisionId/restore` 恢复指定版本。
- 在保存后显示 A08 返回的 warnings，尤其是 dangling derive target。
- 保持文章/项目编辑、内容列表、批量状态、轮播槽位、页面排布、图片库和健康面板原有流程继续走原接口。

## files_created_or_changed

- `admin/index.html`
- `admin/admin.js`
- `admin/admin.css`
- `docs/codex-workline/slices/S05_cms_knowledge_node_workflow_handoff.md`

`admin/course-paths.html` 已读取；本轮无需修改。

## decisions

- 选择“扩展现有编辑器 + 新增推导节点专用列表视图”，避免复制 Markdown 预览、上传和 CSRF 管道。
- 新建推导节点时用 `slug` 作为默认 `id`；编辑已有节点时保留原 `id`，允许按 A08 规则更新 slug。
- 推导节点不进入首页轮播；轮播 UI 与 featured 批量逻辑显式排除 `knowledge_node`。
- `publishStatus='archived'` 只在推导节点模式可选，避免文章/项目误提交后端不支持的状态。
- 公开范围使用 A08 的 `public|unlisted|private`，并在 CMS 中显示直观状态提示。
- 没有实现 `public_focus_mode` 管理控件，因为本 slice 不能新增后端 site_settings API；该能力应等待后续明确打开 API/CMS 配置范围。

## risks

- 未做浏览器截图回归；本轮通过静态语法、API、Markdown 和治理检查验证。正式视觉/交互证据应由后续回归 slice 执行。
- 专用推导节点列表只能显示 admin list payload 中已有字段；详细 links/backlinks 与 revisions 会在打开节点详情时加载。
- 访客端 `derive.html?slug=<slug>` 仍未实现，公开点击体验等待 S06。
- `public_focus_mode` CMS 控件因缺少已开放 API 被记录为缺口，未越权修改 `server.js` 或数据库。

## tests_or_checks

- `npm.cmd run codex:handoff`: passed；路由到 `A10_CmsKnowledgeNodeWorkflow`。
- `npm.cmd run codex:contract`: 变更前 passed，`420 passed, 0 warnings, 0 failures`。
- `node --check admin/admin.js`: passed。
- `git diff --check -- admin/index.html admin/admin.js admin/admin.css`: exit code 0；仅出现 LF 将在 Git 触碰时按 CRLF 归一化的提示。
- `npm.cmd run test:markdown`: passed，`Markdown renderer and MD2File DOCX regression tests passed.`
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-api.ps1`: passed；isolated base URL `http://127.0.0.1:5497`，结果包含 `publicKnowledgeNodes=1`、`knowledge node public/admin boundary`、CSRF、上传、post/project CRUD、export、sitemap、rss、logout。
- `npm.cmd run codex:contract`: 变更和 handoff 后 passed，`420 passed, 0 warnings, 0 failures`。
- `git diff --check -- admin/index.html admin/admin.js admin/admin.css docs/codex-workline/slices/S05_cms_knowledge_node_workflow_handoff.md`: exit code 0；仅出现 LF 将在 Git 触碰时按 CRLF 归一化的提示。

未修改 `server.js`、`lib/`、`data/markdown-renderer.js`、`tools/`、迁移、访客端页面、SEO、发布脚本、当前数据库/runtime data、生产环境或 Git 状态。

## next_handoff

返回 A00 验收 S05，并决定是否打开 S06 访客端推导页与聚焦模式。建议下一轮短提示：

```text
Agent 00 Project Director（项目导演：验收 S05 CMS 推导节点工作流并决定下一门禁）：请进入 E:\Project\2607-LarkixWeb，运行 npm.cmd run codex:handoff，读取 docs/codex-workline/slices/S05_cms_knowledge_node_workflow_handoff.md，验收 A10 输出；不得运行生产迁移、不得修改访客前端/API/数据库/发布/Git，除非治理文件明确打开下一步。
```
