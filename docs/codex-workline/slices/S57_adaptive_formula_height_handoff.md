# S57 Adaptive Formula Height Handoff

Generated by: `A66_AdaptiveFormulaHeight`

## status

`complete`，等待 `A00_ProjectDirector` 执行 Wave 1 验收；不得据此提前开启 S58。

## scope_completed

- 在共享 `LarkixMath` 中增加块级公式真实边界测量器，按 KaTeX 元素高度写入最小块尺寸和 6–14 px 自适应上下安全留白。
- 覆盖文章、独立公式/推导正文、CMS 公式卡与预览、文章预览及 MD2File 预览；块级容器不再使用纵向滚动，行内数学仍沿用自然行盒与 baseline 对齐。
- 字体完成加载、窗口变化、主题属性变化和内容 DOM 变化后通过单帧合并调度重测；ResizeObserver 只观察数学内容，并以 0.5 px 阈值判断有效变化。
- 图谱节点在公式真实尺寸改变后以 1 px 阈值重建 preset 布局，保留 pan/zoom，并同步节点与连线；加入 rebuilding/单帧门闩避免观察器循环。
- 增加固定普通、根号分式、整体根号、嵌套分式、积分、矩阵、cases、boxed 的 Chrome/Edge 无头浏览器尺寸回归。

## files_created_or_changed

- `data/math-renderer.js`
- `styles/20-content.css`
- `admin/admin.css`
- `formula-graph.js`
- `scripts/test-adaptive-formula-height.js`
- `docs/codex-workline/slices/S57_adaptive_formula_height_handoff.md`

## decisions

- 只对块级公式启用高度测量，不修改 `.markdown-math-inline` 的布局、padding 或基线规则。
- 公式高度取实际 `.katex` bounding box；安全留白随内容高度变化并限制在 6–14 px，避免统一超大固定高度。
- 共享块公式使用 `overflow: visible`，从根本上消除横向 overflow 导致纵向 computed overflow 被联动计算为 `auto` 的浏览器行为；未改变 LaTeX、公式关系、API 或数据。
- 图谱重排仅在节点宽或高变化超过 1 px 时执行，尺寸稳定时不重建。

## risks

- 极端超宽且不可断行的公式不再依赖公式块自身的滚动区；现有页面宽度策略仍需在 S60 全站回归中持续观察页面级横向表现。
- 本次未修改不存在的 `styles/40-formula.css`，也未修改未列入 may edit 的 `styles/26-inline-math.css`；以更高特异性的共享块级契约覆盖其旧纵向 overflow 行为。
- 工作树原有大量非本任务文件状态；本任务仅触碰 brief 明确允许的六个文件，未操作 A65 契约文件。

## tests_or_checks

- `node scripts/test-adaptive-formula-height.js`：通过；8 类固定公式 × 文章/CMS/MD2File × 明暗主题 × 390/1366 宽度，共 12 个表面/主题/宽度组合；无纵向滚动范围，上下安全留白满足阈值，高公式高度大于普通公式。
- `npm.cmd run test:math-rendering`：通过。
- `node scripts/test-inline-math-layout.js`：通过。
- `npm.cmd run test:formula-map-flow-layout`：通过。
- `npm.cmd run test:formula-marker-graph-ui`：通过。
- `npm.cmd run test:markdown`：通过，含 MD2File DOCX 回归。
- `npm.cmd run codex:contract`：1226 passed，0 warnings，0 failures。
- `git diff --check -- <S57 files>`：通过；仅出现仓库既有的 LF/CRLF 提示，无空白错误。

## next_handoff

直接回传 `A00_ProjectDirector`：请同时等待并核验 A65 的 S56 交接，再执行 `A00_wave1_acceptance`。只有 S56 与 S57 均验收通过后，才可按派工序列开启 `S58_formula_metadata_management`。

未执行 Git staging、commit、push、部署、迁移、当前/生产数据、云、服务或密钥写入。
