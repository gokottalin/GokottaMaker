# S01 Data Model Migration Contract Handoff

Generated by: A06_DataModelMigrationContract
Workspace: `E:/Project/2607-LarkixWeb`
Recorded at: `2026-07-09`

## status

complete

## scope_completed

- Ran `npm.cmd run codex:handoff` and followed `agents/A06_DataModelMigrationContract/brief.md`.
- Read current governance, S00 handoff, implementation slices, migration runner, current content/audit/validator code, migration files, and prior Agent contracts relevant to schema, API, CMS, Markdown/DOCX, QA, release, and Git gates.
- Defined the V0 data-model and migration contract for:
  - `knowledge_nodes`
  - `knowledge_node_revisions`
  - `knowledge_links`
  - `site_settings.public_focus_mode`
  - audit action names in existing `audit_logs`
- Defined migration boundaries, rollback assumptions, isolated `DATA_DIR` verification, downstream needs, and S02 stop conditions.
- Kept S01 design-only. No source code, migration file, database file, runtime data, upload, release script, or Git state was changed.

## files_created_or_changed

- `docs/codex-workline/slices/S01_data_model_migration_contract.md`
- `docs/codex-workline/slices/S01_data_model_migration_contract_handoff.md`

## decisions

- Use dedicated `knowledge_nodes`; do not overload `posts` or `projects`.
- Use dedicated `knowledge_node_revisions`; do not alter `content_revisions.content_type` in V0.
- Persist `knowledge_links` in V0 with dangling targets allowed and public draft/private/deleted leakage forbidden.
- Add `visibility_status` alongside `publish_status`, matching Agent00's canonical `publishStatus` / `visibilityStatus` decision.
- Store focus mode as `site_settings` key `public_focus_mode`; default `enabled:false`.
- Future migration files should be additive and numbered:
  - `011_knowledge_nodes.js`
  - `012_knowledge_links.js`
  - `013_public_focus_mode_default.js`
- S02 must stay migration/data-model scoped. API, CMS, Markdown/DOCX, frontend, SEO, release, and Git work remain later slices.

## risks

- Agent2A's earlier draft schema used only `draft/published`; this S01 contract follows the later Agent00/Agent3 canonical status decision and includes `archived` plus `visibility_status`.
- The existing migration runner has no `down`; rollback depends on additive compatibility, code rollback tolerance, and Agent1 backup/restore gate.
- `knowledge_links` V0 expands implementation scope; keeping historical post backfill out of S02 is important to avoid hidden source-code/API coupling.
- `public_focus_mode` must remain disabled by default; enabling it during migration would silently change public site behavior.
- The current working tree already has many pre-existing untracked governance files; S01 did not stage or alter Git state.

## tests_or_checks

- `npm.cmd run codex:handoff`: passed; routed to `A06_DataModelMigrationContract`.
- `npm.cmd run codex:contract`: passed before writing S01 contract, `299 passed, 0 warnings, 0 failures`.
- `npm.cmd run codex:contract`: passed after writing S01 contract and handoff, `299 passed, 0 warnings, 0 failures`.
- No migration, API smoke, backup/restore, browser, production, or Git operation was run because S01 is contract-design only.

## next_handoff

Next task should remain gated by A00/Owner. If implementation is opened, start S02 only as a migration implementation slice:

```text
Agent 07 Data Model Migration Implementation（数据迁移实现：仅在门禁打开后落地迁移）：请进入 E:\Project\2607-LarkixWeb，运行 npm.cmd run codex:handoff，然后按输出的 Next Agent brief 执行；先读取 docs/codex-workline/slices/S01_data_model_migration_contract.md，使用隔离 DATA_DIR，不运行生产迁移，不 staging/commit/push。
```

S02 must stop before editing `server.js`, CMS, visitor frontend, Markdown/DOCX renderer, release scripts, runtime data, or Git state unless governance explicitly opens that scope.
