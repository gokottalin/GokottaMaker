# S11 Regression Evidence Matrix Handoff

Generated by: `A16_RegressionEvidenceMatrix`
Workspace: `E:/Project/2607-LarkixWeb`
Recorded at: `2026-07-27`

## status

`complete`

S11 evidence-only 回归已完成：14 项主回归命令与 3 项 closeout 命令全部通过，
9 个隔离浏览器场景全部通过，
无命令失败、超时或 release-blocking 问题。浏览器能力限制、空数据状态和本轮
只读 UI 边界均已诚实记录，没有把未执行或不受支持的检查记为通过。

## scope_completed

- 完整读取根治理门禁、A16 brief、S01–S10 与 S13–S17 当前交接、
  `scripts/verify-api.ps1`、Markdown/DOCX 测试、package scripts，以及
  Agent20 最小证据规范和 Batch 1–9 回归矩阵。
- 运行全部已验收 feature-specific package tests：
  - 公式库。
  - 文章公式创作。
  - 公式引用版本决策。
  - 线性推导图。
  - 聚焦范围门禁。
  - 轮播聚焦缓冲。
  - 计算书。
  - Markdown/DOCX。
- 运行隔离 API、Git Bash shell 语法、内容同步 self-test、版本、治理资源检查
  与 whitespace 检查。
- 使用 Browser skill 在新建隔离 `DATA_DIR` 和独立端口上完成真实 CMS 登录、
  访客页、聚焦状态、轮播缓冲、公式库空状态、桌面/半宽/移动响应式、横向
  溢出和 console 检查。
- 关闭隔离服务，重置 viewport，finalize 浏览器标签，并按精确 TEMP 路径
  删除隔离数据。

## files_created_or_changed

- `docs/codex-workline/regression_results.json`
- `docs/codex-workline/slices/S11_regression_evidence_matrix_handoff.md`

未修改业务代码、测试、预期结果、migration、当前/生产数据库、云端、部署脚本
行为或 Git 状态。

## command_summary

命令总计：17（主回归 14 + closeout 3）；通过：17；失败：0；超时：0；
累计计时：`33153 ms`。

| 命令 | Exit | Duration | 隔离与简述 |
| --- | ---: | ---: | --- |
| `npm.cmd run test:formula-catalog` | 0 | 1864 ms | TEMP 隔离；60 卡、9 分类、修订/归档/分页/快照/API/访客通过 |
| `npm.cmd run test:article-formula-authoring` | 0 | 1474 ms | TEMP 隔离；未绑定 LaTeX、绑定、原子建卡与回滚通过 |
| `npm.cmd run test:formula-reference-versioning` | 0 | 1406 ms | TEMP 隔离；逐文章待决与保留/采用/另建通过 |
| `npm.cmd run test:linear-derivation-graph` | 0 | 1579 ms | TEMP 隔离；汇聚、唯一下一阶、替换、循环/归档通过 |
| `npm.cmd run test:focus-mode` | 0 | 4403 ms | TEMP 隔离；默认开启、范围、404、关闭/重启/SEO 通过 |
| `npm.cmd run test:carousel-focus-buffer` | 0 | 3789 ms | TEMP 隔离；选择性缓冲、无自动恢复、显式槽位与冲突通过 |
| `npm.cmd run test:calculation-book` | 0 | 2331 ms | TEMP 隔离预览；Schema、追溯、Mathcad/Larkix、一致性通过 |
| `npm.cmd run test:markdown` | 0 | 632 ms | 进程内；Markdown 与 MD2File DOCX 回归通过 |
| `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-api.ps1` | 0 | 2179 ms | `.verify-api-data` 隔离并已清理；API 综合回归通过 |
| `"C:\Program Files\Git\bin\bash.exe" -n scripts/content-sync-cloud.sh scripts/deploy-update.sh scripts/backup-linux.sh scripts/restore-linux.sh` | 0 | 83 ms | 只解析四脚本语法 |
| `"C:\Program Files\Git\bin\bash.exe" scripts/content-sync-cloud.sh --self-test` | 0 | 10515 ms | `mktemp -d`；零网络/零 dry-run 写入、幂等和事务回滚通过 |
| `npm.cmd run check:version` | 0 | 728 ms | `V2.5.1+20260726-1650` |
| `npm.cmd run codex:check` | 0 | 953 ms | contract 590/0/0；resource 27 pass、12 warning、0 failure |
| `git diff --check` | 0 | 123 ms | 无 whitespace error；只有 LF→CRLF 提示 |
| `node -e JSON/UTF-8 validation for regression_results.json` | 0 | 115 ms | UTF-8 无 BOM、JSON 可解析、主矩阵 14 项与浏览器 9 项 |
| `git diff --check -- docs/codex-workline/regression_results.json docs/codex-workline/slices/S11_regression_evidence_matrix_handoff.md` | 0 | 55 ms | 两个允许输出无 whitespace error |
| `npm.cmd run codex:contract` | 0 | 924 ms | 落盘后 590 passed、0 warnings、0 failures |

`codex:check` 的 12 个 warning 均为既有 `1.4–2.0 MB` 图片大文件提示，不是
治理或业务回归失败。完整逐命令结果见
`docs/codex-workline/regression_results.json`。

## browser_matrix

隔离环境：

- URL：`http://127.0.0.1:5733`
- PID：`28376`
- Version：`V2.5.1+20260726-1650`
- Commit：`7d6200c`
- DATA_DIR：
  `C:\Users\10731\AppData\Local\Temp\larkix-a16-browser-a70c5bb91b79491b961c822b84c1c6df`
- 主题：暗色；页面按钮可访问名称为“切换日间模式”。

| Viewport | 页面 | 状态与布局 | Overflow | Console |
| --- | --- | --- | --- | --- |
| 1280×900 | 访客 Maker 首页 | H1 电子基础；导航/三个入口顺序正确；管理端链接隐藏 | 1265=1265 | 0 error / 0 warning |
| 1280×900 | 公开推导索引 | 新数据无节点时显示安全空状态，不白屏 | 1265=1265 | 0 / 0 |
| 1280×900 | CMS 聚焦 | 唯一开关开启；文章 1/4、项目 4/4；两列 846.2/299px | 1265=1265 | 0 / 0 |
| 1280×900 | CMS 轮播 | 活跃 1/4、缓冲 3；三个恢复按钮均因聚焦越界禁用 | 1265=1265 | 0 / 0 |
| 720×900 | 访客 Maker 首页 | 三个入口为 681px 单列 | 705=705 | 0 / 0 |
| 720×900 | CMS 轮播 | 活跃/缓冲卡单列；内部操作区未越界 | 705=705 | 0 / 0 |
| 430×900 | 访客 Maker 首页 | 三个入口为 391px 单列；管理端链接隐藏 | 415=415 | 0 / 0 |
| 430×900 | CMS 轮播 | 聚焦 349.6px 单列；缓冲卡 320px、操作区/选择器约 290px | 415=415 | 0 / 0 |
| 430×900 | CMS 公式库 | 安全空状态；搜索/标签/状态/备份导入控件约 358px 单列 | 415=415 | 0 / 0 |

清理结果：

- viewport reset：完成。
- 浏览器 tabs finalize：完成。
- PID 28376：已停止。
- 端口 5733：无监听。
- 精确 TEMP DATA_DIR：已删除。
- `.verify-api-data`：不存在。

## decisions

- 结果只依据本轮实际执行证据；没有沿用旧 handoff 的 pass 作为本轮 pass。
- `codex:check` 的资源 warning 与命令失败分开记录，不把 warning 隐藏。
- 浏览器烟测保持隔离且只读；聚焦切换、轮播恢复、公式导入等写路径由已通过的
  隔离集成测试证明，本轮不重复制造 UI 测试数据。
- 新隔离数据下公式库和公开推导列表为空，因此浏览器验证安全空状态；填充数据
  的 CRUD、分页、版本、路由由 package tests 和 API 覆盖。
- 未生成额外截图/日志文件，避免超出 A16 两个允许输出；关键 DOM、宽度、状态
  和 console 结果已结构化写入 regression JSON。

## risks_and_limitations

- 浏览器能力不支持 `networkidle` 等待；首次调用立即返回“不支持”，未发生
  浏览器连接超时。后续使用 `domcontentloaded` 并做定向 DOM/console 状态检查。
- 浏览器未实际提交聚焦开关、轮播恢复或公式导入；这些写路径的隔离集成测试均
  已通过。本限制为 non-blocking。
- `test:calculation-book` 更新了两册
  `generated/isolated-preview-report.json` 的文件时间；随后针对两文件的
  `git status`、`git diff` 和 `git diff --numstat` 均为空，证明字节内容
  未变化。按门禁没有回退或改写这些非允许输出。
- 一次额外 closeout 包装命令误用了 Windows PowerShell 不支持的
  `ConvertFrom-Json -Depth`，该无效本地验证调用 exit 1、未改文件，也没有
  计入 17 项 pass。随后 Node UTF-8/JSON/矩阵计数验证 exit 0；该事件属于
  tooling-only，不是产品或 release-blocking 回归失败。
- 本轮没有当前/生产数据、真实凭据、云端或 Linux 服务账户证据；不能把本交接
  解释为生产发布批准。

## protected_boundaries

- 当前数据库读取：否。
- 当前数据库写入：否。
- 生产数据：未触碰。
- 真实凭据：未读取。
- 云端/SSH/外部存储：未连接。
- 部署、systemctl、生产 health、restore：未执行。
- Git staging、commit、push：未执行。

## tests_or_checks

机器可读证据：
`docs/codex-workline/regression_results.json`。

JSON 记录了每项命令的精确 invocation、exit code、duration、隔离方式和简述，
以及 9 项浏览器矩阵、cleanup、limitations 和 protected boundaries。

## next_handoff

交回 `A00_ProjectDirector` 验收 S11。A00 应复核：

1. 14/14 主回归和 3/3 closeout 命令均为本轮实际执行且 exit code 0；
2. `codex:check` 12 个 resource warning 的非阻断分类是否接受；
3. 1280/720/430 浏览器状态、console、overflow 和清理证据；
4. Browser `networkidle` 不支持、隔离空公式/推导目录、只读 UI 烟测三个限制；
5. 当前/生产数据、云、部署与 Git 边界均保持关闭。

A00 接受后才可决定是否打开 `A17_ReleaseGitGate`。A16 不执行生产发布、
Git staging、commit、push、当前数据 migration 或云端操作。
