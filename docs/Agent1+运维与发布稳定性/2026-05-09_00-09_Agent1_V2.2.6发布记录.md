# Agent1 V2.2.6 发布记录

记录时间：2026-05-09 00:09  
目标版本：`V2.2.6+20260508-2254`  
目标提交：`98387c3`  
线上地址：`http://81.71.156.122:4173/`

## 1. 已阅读依据

- `docs/agent-current-handoff.md`
- `docs/2026-05-08_五Agent轮播治理与项目重评估.md`
- `docs/Agent1+运维与发布稳定性/2026-05-08_Agent1_轮播治理发布任务.md`

说明：本地未发现 `2026-05-09` 命名的任务文件；当前有效入口是 2026-05-08 23:13 的轮播治理总控文档。

## 2. 本地发布前验证

已执行：

```powershell
npm.cmd run check:version
node --check admin\admin.js
node --check lib\validators.js
node --experimental-sqlite --check server.js
powershell -ExecutionPolicy Bypass -File scripts\verify-api.ps1
git diff --cached --check
```

结果：

- 版本检查通过：`V2.2.6+20260508-2254`
- API 回归通过：登录、CSRF、轮播排序越界、上传、文章 CRUD、项目 CRUD、导出、Sitemap、RSS、退出登录
- 提交前 diff 检查通过

## 3. Git 发布状态

已提交并推送：

```text
commit: 98387c3
message: Publish V2.2.6 carousel governance
origin/main: 98387c3
```

本地状态：

```text
## main...origin/main
```

## 4. 当前云端状态

发布前云端 `/healthz`：

```json
{
  "ok": true,
  "name": "LarkixMaker",
  "version": "V2.2.5",
  "build": "20260507-1608",
  "versionLabel": "V2.2.5+20260507-1608",
  "gitCommit": "3d1e810",
  "node": "v22.11.0",
  "data": {
    "databaseBytes": 4312568,
    "uploadsBytes": 364468,
    "uploadsFiles": 3
  }
}
```

结论：云端尚未部署 V2.2.6，当前仍是 `V2.2.5+20260507-1608`。

## 5. 等待服务器执行

当前 Codex 环境无法直接 SSH 到服务器：

```text
ubuntu@81.71.156.122: Permission denied (publickey)
```

请在服务器执行：

```bash
cd /opt/LarkixMaker
git status -sb
git rev-parse --short HEAD
git rev-parse --short origin/main
sudo bash scripts/deploy-update.sh
curl -fsS http://127.0.0.1:4173/healthz
LATEST="$(ls -1dt /srv/gokottamaker-backups/* | head -n1)"
echo "$LATEST"
cat "$LATEST/manifest.txt"
sudo ls -lah /srv/gokottamaker-data/uploads
```

回传以上输出后，Agent1 继续确认：

- 云端版本是否为 `V2.2.6+20260508-2254`
- 云端 commit 是否为 `98387c3`
- 部署前备份目录与 manifest 是否存在
- 上传目录文件数量与大小是否正常

## 6. 云端发布成功确认

用户回传部署结果：

```text
Restart service: success
Active: active (running) since Sat 2026-05-09 00:42:32 CST
Backup: /srv/gokottamaker-backups/2026-05-09_00-42-31
```

部署后云端 `/healthz`：

```json
{
  "ok": true,
  "name": "LarkixMaker",
  "version": "V2.2.6",
  "build": "20260508-2254",
  "versionLabel": "V2.2.6+20260508-2254",
  "gitCommit": "unknown",
  "node": "v22.11.0",
  "data": {
    "databaseBytes": 4312568,
    "uploadsBytes": 364468,
    "uploadsFiles": 3
  }
}
```

Agent1 本地再次请求公网 `/healthz`，确认仍返回：

```text
versionLabel=V2.2.6+20260508-2254
gitCommit=unknown
databaseBytes=4312568
uploadsBytes=364468
uploadsFiles=3
```

部署结论：

| 项 | 结果 |
|---|---|
| 云端版本 | 已更新到 `V2.2.6+20260508-2254` |
| 服务状态 | `active (running)`，已重启 |
| 备份 | 已创建 `/srv/gokottamaker-backups/2026-05-09_00-42-31` |
| 上传目录 | 保持 `3` 个文件，`364468` bytes |
| 数据库 | `4312568` bytes |
| Git commit | `/healthz` 返回 `unknown`，需单独排查审计字段 |

说明：`gitCommit=unknown` 不影响当前 V2.2.6 运行版本确认，但会影响发布审计。当前服务端逻辑会优先读取 `GIT_COMMIT` 环境变量，否则在应用目录执行 `git rev-parse --short HEAD`。需要确认 systemd 运行环境中是否能读取 `.git` 或是否应显式写入 `GIT_COMMIT=98387c3`。

## 7. 回滚预案

## 7. GIT_COMMIT 审计字段修复

用户已在 `/etc/gokottamaker.env` 增加或更新：

```text
GIT_COMMIT=98387c3
```

执行重启后，生产 `/healthz` 返回：

```json
{
  "ok": true,
  "name": "LarkixMaker",
  "version": "V2.2.6",
  "build": "20260508-2254",
  "versionLabel": "V2.2.6+20260508-2254",
  "gitCommit": "98387c3",
  "node": "v22.11.0",
  "data": {
    "databaseBytes": 4316664,
    "uploadsBytes": 364468,
    "uploadsFiles": 3
  }
}
```

Agent1 从公网再次确认：

```text
versionLabel=V2.2.6+20260508-2254
gitCommit=98387c3
uploadsFiles=3
uploadsBytes=364468
```

结论：版本、commit 审计字段、上传目录统计均已确认。V2.2.6 云端发布闭环完成。

## 8. 回滚预案

如果部署失败或 `/healthz` 不正确：

```bash
cd /opt/LarkixMaker
bash scripts/rollback.sh 3d1e810
curl -fsS http://127.0.0.1:4173/healthz
```
