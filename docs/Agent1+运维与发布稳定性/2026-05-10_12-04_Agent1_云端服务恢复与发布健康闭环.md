# Agent1 云端服务恢复与发布健康闭环

记录时间：2026-05-10 12:04  
目标版本：`V2.4.1+20260509-2145`  
目标提交：`6057470`  
云端地址：`http://81.71.156.122:4173/`

## 1. 已阅读今日文档

- `docs/2026-05-10_五Agent升级评分与P0任务分配.md`
- `D:\Project\26-WEB\项目报告\2026-05-10_01-07_项目升级评估与P0分工_汇报.md`
- `docs/agent-current-handoff.md`

今日文档中 Agent1 的 P0 目标：

- 恢复云端服务。
- 确认 `/healthz` 返回 `ok:true`。
- 确认版本为 `V2.4.1+20260509-2145`。
- 确认提交审计为 `6057470`。
- 确认数据库、上传目录和备份。

## 2. 当前云端健康状态

Agent1 从本地请求公网：

```powershell
curl.exe -fsS --max-time 10 http://81.71.156.122:4173/healthz
```

返回：

```json
{
  "ok": true,
  "name": "GokottaMaker",
  "version": "V2.4.1",
  "build": "20260509-2145",
  "versionLabel": "V2.4.1+20260509-2145",
  "gitCommit": "98387c3",
  "node": "v22.11.0",
  "data": {
    "databaseBytes": 4316664,
    "uploadsBytes": 364468,
    "uploadsFiles": 3
  }
}
```

结论：

| 项 | 状态 |
|---|---|
| 云端服务 | 已恢复，公网 `/healthz` 返回 `ok:true` |
| 版本 | 已是 `V2.4.1+20260509-2145` |
| 数据库 | `4316664` bytes |
| 上传目录 | `3` 个文件，`364468` bytes |
| 提交审计 | 当前仍是旧值 `98387c3`，需更新为 `6057470` |

## 3. 上传图片公开访问验证

公开内容中存在上传图片：

```text
./uploads/2026-05-07-c9d0345a7ce35d1a.jpg
```

Agent1 请求：

```powershell
curl.exe -I --max-time 10 http://81.71.156.122:4173/uploads/2026-05-07-c9d0345a7ce35d1a.jpg
```

返回摘要：

```text
HTTP/1.1 200 OK
Content-Type: image/jpeg
Cache-Control: public, max-age=604800
```

结论：云端上传图片公开访问正常。

## 4. 待服务器侧完成的健康闭环

当前还差两件必须在服务器上完成的动作：

1. 更新 `/etc/gokottamaker.env` 中的 `GIT_COMMIT=6057470`，让 `/healthz.gitCommit` 与 V2.4.1 发布提交一致。
2. 在服务器执行备份、manifest、上传目录和轮播巡检命令。

推荐命令：

```bash
cd /opt/GokottaMaker

sudo cp /etc/gokottamaker.env /etc/gokottamaker.env.bak.$(date +%Y%m%d-%H%M%S)
if sudo grep -q '^GIT_COMMIT=' /etc/gokottamaker.env; then
  sudo sed -i 's/^GIT_COMMIT=.*/GIT_COMMIT=6057470/' /etc/gokottamaker.env
else
  echo 'GIT_COMMIT=6057470' | sudo tee -a /etc/gokottamaker.env
fi

sudo systemctl restart gokottamaker
sleep 2
curl -fsS http://127.0.0.1:4173/healthz

LATEST="$(ls -1dt /srv/gokottamaker-backups/* | head -n1)"
echo "$LATEST"
cat "$LATEST/manifest.txt"
sudo ls -lah /srv/gokottamaker-data/uploads

bash scripts/check-carousel-cloud.sh
```

预期：

```text
versionLabel=V2.4.1+20260509-2145
gitCommit=6057470
uploadsFiles=3
uploadsBytes=364468
```

## 5. 服务器回传闭环结果

用户回传的服务器 `/healthz`：

```json
{
  "ok": true,
  "name": "GokottaMaker",
  "version": "V2.4.1",
  "build": "20260509-2145",
  "versionLabel": "V2.4.1+20260509-2145",
  "gitCommit": "6057470",
  "node": "v22.11.0",
  "data": {
    "databaseBytes": 4316664,
    "uploadsBytes": 364468,
    "uploadsFiles": 3
  }
}
```

最新备份：

```text
/srv/gokottamaker-backups/2026-05-10_03-17-01
```

备份 manifest：

```text
name=GokottaMaker backup
created_at=2026-05-10T03:17:01+08:00
source_dir=/srv/gokottamaker-data
backup_dir=/srv/gokottamaker-backups/2026-05-10_03-17-01
database_file=/srv/gokottamaker-backups/2026-05-10_03-17-01/database/gokottamaker.sqlite
source_database_integrity=skipped-sqlite3-missing
backup_database_integrity=skipped-sqlite3-missing
source_database_bytes=159744
backup_database_bytes=159744
source_uploads_files=3
backup_uploads_files=3
backup_total_bytes=4693866
```

上传目录：

```text
2026-05-06-2ebc47618a677c97.jpg  48K
2026-05-07-c9d0345a7ce35d1a.jpg 166K
2026-05-07-d9abc0537283c821.jpg 143K
```

结论：

| 项 | 状态 |
|---|---|
| 云端服务 | `ok:true` |
| 版本 | `V2.4.1+20260509-2145` |
| 提交审计 | `6057470` |
| 备份 | 已创建并有 manifest |
| 上传目录 | 3 个文件，与 `/healthz` 统计一致 |

补充问题：`bash scripts/check-carousel-cloud.sh` 返回 `node: bad option: --experimental-sqlite`。原因是服务器 shell 默认 `node` 不是服务使用的 `/opt/node22/bin/node`。已在本地修复 `check-carousel-cloud.sh` 和 `cleanup-carousel-cloud.sh`，后续脚本会优先使用 `/opt/node22/bin/node`，也支持 `NODE_BIN=/path/to/node` 显式指定。

## 6. 当前结论

云端服务已经从今日文档记录的 502 状态恢复，公网 `/healthz` 可用，版本正确，提交审计正确，上传图片公开访问正常，备份 manifest 和上传目录均已确认。

剩余项：轮播云端巡检需要使用 Node 22 重新执行。当前不影响 Agent1 发布健康闭环，但会影响 Agent2 的轮播数据一致性验收。

## 7. SQLite 备份完整性闭环

用户已在服务器安装 `sqlite3`，随后重新执行备份与恢复校验。

新备份 manifest 关键结果：

```text
source_database_integrity=ok
backup_database_integrity=ok
source_database_bytes=159744
backup_database_bytes=159744
source_uploads_files=3
backup_uploads_files=3
backup_total_bytes=536904
```

checksum 校验：

```text
./database/gokottamaker.sqlite: OK
./manifest.txt: OK
./uploads/2026-05-06-2ebc47618a677c97.jpg: OK
./uploads/2026-05-07-c9d0345a7ce35d1a.jpg: OK
./uploads/2026-05-07-d9abc0537283c821.jpg: OK
```

恢复校验：

```text
Backup validation passed. SQLite integrity: ok
Safety copy created: /srv/gokottamaker-data.before-restore-2026-05-10_12-23
Restore completed from: /srv/gokottamaker-backups/2026-05-10_12-23-34
Restored SQLite integrity: ok
```

结论：

| 项 | 状态 |
|---|---|
| sqlite3 | 已安装 |
| 源数据库完整性 | `ok` |
| 备份数据库完整性 | `ok` |
| checksum | 全部 OK |
| 恢复后完整性 | `ok` |

注意：本次原计划 dry-run，但输出显示执行了真实恢复。原因是 `DRY_RUN=true sudo ...` 在部分 sudo 配置下不会保留环境变量。由于本次恢复源是刚创建的新备份，且恢复后 SQLite 完整性为 `ok`，没有发现数据风险。

已在本地修复 `scripts/restore-linux.sh`，后续可使用显式参数避免环境变量被 sudo 丢弃：

```bash
sudo bash scripts/restore-linux.sh --dry-run /srv/gokottamaker-backups/YYYY-MM-DD_HH-MM-SS
```

至此 Agent1 的云端服务、版本审计、上传目录、备份 manifest、SQLite 完整性和恢复校验闭环完成。

## 8. 配合 Agent2 上传图片持久化与公开访问收尾

依据 Agent2 文档：

```text
docs/Agent2+后端与数据模型/2026-05-10_Agent2_轮播一致性与上传公开访问验证.md
```

Agent2 已确认云端 `/api/content` 中存在上传封面引用：

```text
./uploads/2026-05-07-c9d0345a7ce35d1a.jpg
```

Agent1 对云端上传目录中的 3 个文件逐一执行公开访问验证：

```powershell
curl.exe -I --max-time 10 http://81.71.156.122:4173/uploads/2026-05-06-2ebc47618a677c97.jpg
curl.exe -I --max-time 10 http://81.71.156.122:4173/uploads/2026-05-07-c9d0345a7ce35d1a.jpg
curl.exe -I --max-time 10 http://81.71.156.122:4173/uploads/2026-05-07-d9abc0537283c821.jpg
```

三项均返回：

```text
HTTP/1.1 200 OK
Content-Type: image/jpeg
Cache-Control: public, max-age=604800
```

结合服务器回传：

```text
uploadsFiles=3
uploadsBytes=364468
backup_uploads_files=3
```

结论：

| 验收项 | 状态 |
|---|---|
| 上传目录持久化 | 通过，`/srv/gokottamaker-data/uploads` 存在 3 个文件 |
| 健康检查统计 | 通过，`uploadsFiles=3`、`uploadsBytes=364468` |
| 备份纳入上传目录 | 通过，`backup_uploads_files=3` |
| 公开 HTTP 访问 | 通过，3 个 `/uploads/*.jpg` 均为 `200 OK` |
| 内容引用图片访问 | 通过，`/api/content` 中上传封面可公开访问 |

Agent1 上传图片持久化与公开访问任务完成。

备注：Agent2 文档仍记录云端轮播数据存在重复 slot 3，需执行 Agent2 的最小清理命令。这属于轮播数据一致性收尾，不影响 Agent1 对上传图片持久化与公开访问的验收结论。
