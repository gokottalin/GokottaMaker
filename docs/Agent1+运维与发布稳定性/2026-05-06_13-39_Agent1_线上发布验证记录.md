# Agent1 线上发布验证记录

验证时间：2026-05-06 13:39
报告下达目标版本：`V2.1.1+20260506-1205`
当前本地目标版本：`V2.1.1+20260506-1341`
目标提交：`d19f940`
当前本地提交：`a68fa3c`
线上地址：`http://81.71.156.122:4173/`
健康检查：`http://81.71.156.122:4173/healthz`

## 1. 验证结论

生产服务器尚未部署到目标版本。

当前公网 `/healthz` 返回：

```json
{
  "ok": true,
  "name": "LarkixMaker",
  "version": "V2.0.0",
  "build": "20260506-0748",
  "versionLabel": "V2.0.0+20260506-0748",
  "gitCommit": "e10561b",
  "node": "v22.11.0",
  "data": {
    "databaseBytes": 3819056,
    "uploadsBytes": 0,
    "uploadsFiles": 0
  }
}
```

与目标版本差异：

| 项 | 目标 | 线上当前 | 结论 |
|---|---|---|---|
| versionLabel | 报告目标 `V2.1.1+20260506-1205`；当前本地 `V2.1.1+20260506-1341` | `V2.0.0+20260506-0748` | 未更新 |
| gitCommit | `d19f940` | `e10561b` | 未更新 |
| Node | 未变更要求 | `v22.11.0` | 正常 |
| healthz | 应返回目标版本 | 返回旧版本 | 需部署 |

## 2. 已执行命令

```powershell
curl.exe -fsS --max-time 10 http://81.71.156.122:4173/healthz
```

结果：请求成功，但版本不是目标版本。

## 3. 需要服务器执行

请在生产服务器执行：

```bash
cd /opt/LarkixMaker
sudo bash scripts/deploy-update.sh
```

执行后请回传以下输出：

```bash
curl -fsS http://127.0.0.1:4173/healthz
sudo systemctl status gokottamaker --no-pager
```

如果部署脚本失败，请同时回传：

```bash
sudo journalctl -u gokottamaker -n 120 --no-pager
```

## 4. 回滚预案

如部署后健康检查失败，可执行：

```bash
cd /opt/LarkixMaker
bash scripts/rollback.sh
```

如果需要回滚到当前线上版本对应提交，可指定：

```bash
cd /opt/LarkixMaker
bash scripts/rollback.sh e10561b
```

## 5. 发布阻塞状态

状态：阻塞在服务器部署执行。
原因：本地无法替代服务器上的 `git pull`、systemd 重启、生产数据备份和真实 `/healthz` 验证。

## 6. 2026-05-06 13:56 用户回传结果

用户回传的生产 `/healthz`：

```json
{
  "ok": true,
  "name": "LarkixMaker",
  "version": "V2.0.0",
  "build": "20260506-0748",
  "versionLabel": "V2.0.0+20260506-0748",
  "gitCommit": "e10561b",
  "node": "v22.11.0",
  "uptimeSeconds": 47460,
  "startedAt": "2026-05-06T00:45:21.800Z",
  "serverTime": "2026-05-06T13:56:21.619Z",
  "data": {
    "databaseBytes": 3819056,
    "uploadsBytes": 0,
    "uploadsFiles": 0
  }
}
```

用户回传的 systemd 状态摘要：

```text
Active: active (running) since Wed 2026-05-06 08:45:21 CST
Main PID: 849169 (node)
Exec: /opt/node22/bin/node --experimental-sqlite /opt/LarkixMaker/server.js
SQLite database: /srv/gokottamaker-data/database/gokottamaker.sqlite
```

结论：

- 生产服务本身是运行状态。
- 生产版本仍为 `V2.0.0+20260506-0748`。
- 生产 commit 仍为 `e10561b`。
- 服务已连续运行约 13 小时，未体现 V2.1.1 部署后的重启。
- 仍需执行或排查 `scripts/deploy-update.sh`。

下一步最短诊断命令：

```bash
cd /opt/LarkixMaker
git status -sb
git rev-parse --short HEAD
git rev-parse --short origin/main
sudo bash scripts/deploy-update.sh
```

如 `deploy-update.sh` 失败，请回传完整输出和：

```bash
sudo journalctl -u gokottamaker -n 120 --no-pager
```

## 7. 2026-05-06 22:19 生产部署成功记录

用户已在生产服务器执行：

```bash
cd /opt/LarkixMaker
git status -sb
git rev-parse --short HEAD
git rev-parse --short origin/main
sudo bash scripts/deploy-update.sh
```

部署前：

```text
git status: ## main...origin/main [behind 3]
local HEAD: e10561b
origin/main: a68fa3c
healthz: V2.0.0+20260506-0748 / e10561b
service state: active
```

备份：

```text
Backup created: /srv/gokottamaker-backups/2026-05-06_22-19-02
Manifest: /srv/gokottamaker-backups/2026-05-06_22-19-02/manifest.txt
```

部署过程：

```text
Fast-forward: e10561b..a68fa3c
Restart service: success
```

部署后 `/healthz`：

```json
{
  "ok": true,
  "name": "LarkixMaker",
  "version": "V2.1.1",
  "build": "20260506-1205",
  "versionLabel": "V2.1.1+20260506-1205",
  "gitCommit": "a68fa3c",
  "node": "v22.11.0",
  "uptimeSeconds": 2,
  "startedAt": "2026-05-06T14:19:04.272Z",
  "serverTime": "2026-05-06T14:19:06.274Z",
  "data": {
    "databaseBytes": 4300280,
    "uploadsBytes": 0,
    "uploadsFiles": 0
  }
}
```

部署比对：

| 项 | 部署前 | 部署后 | 结论 |
|---|---|---|---|
| commit | `e10561b` | `a68fa3c` | 已更新 |
| versionLabel | `V2.0.0+20260506-0748` | `V2.1.1+20260506-1205` | 已更新 |
| service | active | active after restart | 正常 |
| backup | 无本次备份 | `/srv/gokottamaker-backups/2026-05-06_22-19-02` | 已创建 |

结论：生产服务器已部署到 `V2.1.1+20260506-1205`，commit `a68fa3c`。本次 Agent1 P0 发布确认闭环完成。

备注：本次 `deploy-update.sh` 输出中的 `File version: unknown` 不影响部署结果，真实版本以 `/healthz` 为准。后续已在本地修正版本解析逻辑，使脚本能兼容更宽松的 `server.js` 写法。
