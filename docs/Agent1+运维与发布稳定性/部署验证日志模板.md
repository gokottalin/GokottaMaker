# 部署验证日志模板

> 每次线上执行 `scripts/deploy-update.sh` 后复制本模板，保存为 `YYYY-MM-DD_HH-mm_Agent1_部署验证日志.md`。

## 1. 基本信息

| 项 | 值 |
|---|---|
| 部署时间 |  |
| 执行人 |  |
| 服务器 | 腾讯云 Ubuntu 22.04 |
| 应用目录 | `/opt/GokottaMaker` |
| 服务名 | `gokottamaker` |
| 线上地址 | `http://81.71.156.122:4173/` |

## 2. 部署前状态

```text
git rev-parse --short HEAD:

versionLabel:

systemctl is-active gokottamaker:

curl /healthz:
```

## 3. 备份信息

```text
Backup created:

Manifest:

source_database_integrity:

backup_database_integrity:

backup_total_bytes:
```

## 4. 部署后状态

```text
git rev-parse --short HEAD:

versionLabel:

systemctl is-active gokottamaker:

curl /healthz:
```

## 5. 版本比对

| 项 | 部署前 | 部署后 | 是否符合预期 |
|---|---|---|---|
| commit |  |  |  |
| versionLabel |  |  |  |
| databaseBytes |  |  |  |
| uploadsFiles |  |  |  |
| latest backup |  |  |  |

## 6. 访问验证

| 页面/接口 | 结果 |
|---|---|
| `/` |  |
| `/admin/` |  |
| `/healthz` |  |
| `/api/admin/health` |  |
| `/rss.xml` |  |
| `/sitemap.xml` |  |

## 7. 异常与处理

```text
异常：

处理：

是否回滚：

回滚目标 commit：
```

## 8. 结论

```text
本次部署结论：

后续动作：
```
