# 发布记录模板

> 每次执行生产发布后复制本模板，命名为 `YYYY-MM-DD_HH-mm_Agent1_发布记录.md`。
> 版本规则：常规小版本发布递增 patch（例如 `V2.4.9 -> V2.4.10`）；阶段性大版本发布递增 minor 并归零 patch（例如 `V2.4.x -> V2.5.0`）。

## 1. 发布信息

| 项 | 内容 |
|---|---|
| 发布时间 |  |
| 执行人 |  |
| 目标版本 |  |
| 目标提交 |  |
| 服务器 |  |
| 应用目录 | `/opt/LarkixMaker` |
| 服务名 | `gokottamaker` |

## 2. 发布前

```text
git status -sb:

git rev-parse --short HEAD:

curl http://127.0.0.1:4173/healthz:

systemctl is-active gokottamaker:
```

## 3. 备份

```text
Backup created:

Manifest:

source_database_integrity:

backup_database_integrity:

backup_total_bytes:
```

## 4. 发布命令

```bash
cd /opt/LarkixMaker
sudo bash scripts/deploy-update.sh
```

## 5. 发布后

```text
git rev-parse --short HEAD:

curl http://127.0.0.1:4173/healthz:

systemctl is-active gokottamaker:

systemctl status gokottamaker --no-pager:
```

## 6. 版本比对

| 项 | 发布前 | 发布后 | 是否符合预期 |
|---|---|---|---|
| versionLabel |  |  |  |
| gitCommit |  |  |  |
| databaseBytes |  |  |  |
| uploadsFiles |  |  |  |
| latest backup |  |  |  |

## 7. 回滚信息

```text
是否回滚：

回滚目标：

回滚命令：

回滚后 healthz：
```

## 8. 结论

```text
发布结论：

遗留风险：

下一步：
```
