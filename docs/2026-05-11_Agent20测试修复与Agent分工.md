# 2026-05-11 Agent20 测试修复与 Agent 分工

## 当前版本

- 目标版本：V2.4.3+20260511-1327
- 测试来源：`docs/Agent20+体验测试与问题上报/2026-05-11_第1次测试问题报告.md`
- 主控处理范围：优先修复今天测试报告里的 P1/P2 可落地问题，并把剩余协作项按原 Agent 职能拆分。

## 主控已修复

1. 详情页非法 ID 回退问题
   - 文件：`post.js`
   - 变更：详情页只在未传 `id` 时使用默认第一项；传入不存在的 `id` 时显示“没有找到内容”，不再静默回退到第一篇公开内容。

2. 规划中项目直链状态
   - 文件：`project.html`
   - 变更：项目详情页使用“默认项目种子 + 服务端公开项目覆盖”的合并集合。规划中项目可被识别，但正文不会渲染，只显示“该项目尚未上线”的访客提示。

3. Markdown 代码高亮破坏 HTML
   - 文件：`data/markdown-renderer.js`
   - 变更：高亮逻辑改为从左到右扫描，不再对已经生成的 `<span class="token-*">` 属性继续替换。

4. GokottaElec 工具缓存号
   - 文件：`tools/gokotta-elec.html`
   - 变更：样式、工具脚本、站点元信息缓存号统一到 `20260511-1327`。

5. 首页“最新教程 / 查看全部”入口
   - 文件：`index.html`、`category.html`
   - 变更：首页入口改为 `category.html?category=all`；分类页新增“全部教程”聚合视图。

6. 版本号同步
   - 文件：`server.js`、`data/site-meta.js`、`package.json`、主要 HTML 引用
   - 变更：统一为 `V2.4.3+20260511-1327`。

## 本地验证

- `node --check data\markdown-renderer.js`
- `node --check post.js`
- `node --experimental-sqlite --check server.js`
- `node scripts/check-version.js`
- Markdown 高亮专项脚本：通过，字符串里的 `token-keyword` 不再破坏 class。
- Browser 路由检查：
  - `post.html?id=not-existing-post`：显示“没有找到内容”。
  - `project.html?id=power-amp`：显示“桌面功率放大器”和“尚未上线”提示。
  - `category.html?category=all`：显示“全部教程”，包含多个方向内容。
  - `index.html`：最新教程“查看全部”指向 `category=all`。
- 备注：Browser 插件的截图接口本次 `Page.captureScreenshot` 超时，但 DOM、标题、URL、控制台检查均通过，未发现相关 error/warn。

## Agent 分工与接口

### Agent1 运维与发布稳定性

- 输入：本次提交后的仓库、版本 `V2.4.3+20260511-1327`、腾讯云现有部署目录 `/opt/GokottaMaker`。
- 任务：
  - 发布前确认云端工作树干净，避免再次被 `server.js` 本地脏改阻塞。
  - 执行 `sudo bash scripts/deploy-update.sh`。
  - 验证 `/api/health` 返回 `V2.4.3+20260511-1327`。
  - 验证 `uploads` 图片和数据库数据未丢失。
- 输出：发布日志、健康检查 JSON、备份目录路径、云端图片访问确认结果。
- 边界：不改业务代码；发现云端脏文件时只备份/隔离，不直接覆盖用户数据。

### Agent2 后端与数据模型

- 输入：`server.js`、`lib/content-store.js`、`migrations`、本次详情页直链修复。
- 任务：
  - 检查 API 是否明确区分 public/draft/planned/development。
  - 防止服务端接口在非法 ID 时返回第一条内容。
  - 复核首页轮播最多 4 个、排序只允许 0-3 的后端约束。
- 输出：数据契约说明、必要的迁移或 API 修正、回归用例。
- 边界：不负责视觉排版；不把未发布正文暴露给访客 API。

### Agent3 管理端 CMS 与内容工作流

- 输入：`admin/index.html`、`admin/admin.js`、本次 `category=all` 与规划中项目行为。
- 任务：
  - 管理端保存/预览需要与访客端 Markdown 渲染器保持一致。
  - 对草稿、规划中项目的访客直链行为给出清晰提示。
  - 内容列表中标记首页轮播 slot 0-3，禁止无上限排序。
- 输出：CMS 交互修正、表单约束、保存后的验证记录。
- 边界：不改访客端大视觉；不改数据库 schema，除非 Agent2 已确认。

### Agent4 访客端体验、视觉与前端架构

- 输入：`index.html`、`category.html`、`project.html`、`post.js`、`styles.css`、本次 Browser 检查结果。
- 任务：
  - 回归详情页空状态、规划中项目提示、全部教程聚合页。
  - 检查桌面与移动端是否有布局异常、空状态是否美观。
  - 如需优化“全部教程”入口文案和视觉层级，与 Agent5 对齐。
- 输出：截图或 DOM 结果、视觉缺陷列表、修复 patch。
- 边界：不变更 CMS 数据结构；不新增内容策略。

### Agent5 内容、SEO 与知识体系

- 输入：新增 `category=all` 路由、首页“查看全部”入口、现有 `sitemap.xml`/`rss.xml` 生成逻辑。
- 任务：
  - 决定 `category=all` 是否加入 sitemap。
  - 检查“全部教程 / Latest Tutorials / 查看全部”命名是否统一。
  - 补充测试报告中未覆盖的内容侧风险，例如空摘要、缺图、标签混乱。
- 输出：SEO 调整建议、必要文案修正、sitemap/rss 回归结果。
- 边界：不改管理端交互；不改发布脚本。

### Agent6 Leo 小程序功能接入

- 输入：`miniapps.html`、`tools/gokotta-elec.html`、`gokotta-elec-core`。
- 任务：
  - 复核 GokottaElec 工具缓存号、工具版本、miniapps 卡片版本是否一致。
  - 如果测试部继续反馈 miniapps 版本错误，按工具真实版本更新展示。
- 输出：miniapps 版本核对表、工具页加载结果。
- 边界：不接管博客/项目详情页。

### Agent20 体验测试与问题上报

- 输入：`V2.4.3+20260511-1327` 本地或云端地址。
- 任务：
  - 复测 BUG-001 到 BUG-004。
  - 对未关闭问题继续用批注截图提交。
  - 对截图接口无法生成的问题，用 DOM/URL/控制台日志替代证据。
- 输出：关闭/重开测试报告。
- 边界：只测试和上报，不直接改代码。

## 后续执行顺序

1. Agent1 先确认云端可安全发布。
2. Agent2 复核 API/数据约束，避免前端补丁掩盖服务端问题。
3. Agent3 对管理端编辑、保存、轮播 slot 做表单层修正。
4. Agent4 做访客端视觉回归。
5. Agent5 更新 SEO 和内容索引。
6. Agent6 Leo 独立复核 miniapps。
7. Agent20 做最终测试回归。
