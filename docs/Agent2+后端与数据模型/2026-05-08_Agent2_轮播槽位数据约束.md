# Agent2 轮播槽位数据约束

时间：2026-05-08 23:13
目标版本：V2.2.6+20260508-2254

## 目标

维护首页轮播的后端约束：最多 4 条，排序只允许 0、1、2、3，同一槽位不能重复。

## 输入

- `server.js`
- `lib/validators.js`
- `lib/content.js`
- `scripts/verify-api.ps1`

## 输出

- API 约束说明。
- 脏数据检查或清理方案。
- 回归测试覆盖越界排序、重复槽位和超过 4 条。

## 当前状态

本轮已经在验证器和 API 层阻止 `featuredOrder > 3`，并阻止超过 4 个首页轮播项。

## 下一步

补充云端数据库巡检脚本，列出当前所有 `featured=1` 的文章和项目，按槽位排序输出。
