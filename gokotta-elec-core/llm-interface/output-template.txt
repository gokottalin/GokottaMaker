电路 <CIRCUIT_ID> 版本 0.1.0。

网络 GND 是 ground，说明=全局参考地。
网络 VCC 是 power，说明=<电源说明>。
网络 VIN 是 input，说明=<输入说明>。
网络 VOUT 是 output，说明=<输出说明>。
网络 N001 是 internal，说明=<内部节点说明>。

器件 V1 是 VOLTAGE_SOURCE_DC，参数{voltage=<电压>}。
器件 R1 是 RESISTOR，参数{resistance=<阻值>}。
器件 Q1 是 BJT_NPN，型号=<型号>，封装=<封装>。

连接 VCC: V1.POS, R1.A。
连接 GND: V1.NEG。
连接 N001: R1.B, Q1.B。

约束 Q1 必须 terminal_connected(C,B,E)。
