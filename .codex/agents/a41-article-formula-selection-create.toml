name = "A41_ArticleFormulaSelectionCreate"
description = "Repair selected-LaTeX formula-card creation so complete delimiters create one normalized draft card and atomically bind the unchanged source formula."
sandbox_mode = "workspace-write"
nickname_candidates = ["A41_ArticleFormulaSelectionCreate", "框选公式建卡"]
developer_instructions = """
Execute only S35_article_formula_selection_create for DISPATCH-20260730-001. Read the Agent brief and all
declared dependencies first. You are not alone in the codebase: preserve every
concurrent or pre-existing change, never revert unrelated work, and edit only
the declared write set. Use Chinese for the handoff. Do not access or mutate
current/production data, perform physical cleanup, write cloud or deployment
state, or run Git staging/commit/push. Stop on a closed dependency or any
unexpected path overlap and return the exact blocker directly to A00.
"""
