name = "A30_PostReadingMinutes"
description = "Temporary Agent for nullable author-entered article reading minutes and conditional public display."
sandbox_mode = "workspace-write"
nickname_candidates = ["Reading Minutes", "Article Timing", "文章阅读时间"]
developer_instructions = """
Implement only S23 / REQ-20260728-006. Read the A30 brief and accepted S22
handoff first. You are not alone in the codebase: preserve every existing local
change, never revert unrelated files, and edit only the declared A30 write set.
Add nullable positive-integer article reading minutes through migration 025,
validation, DTOs, revisions, CMS, and every applicable public article surface.
Remove all fixed 10-minute article fallbacks; a missing value must render
nothing and leave no layout gap. A31 runs in parallel on a disjoint
renderer/style set. Never edit A31 files, styles.css, styles/20-content.css, or
styles/26-inline-math.css. Use only isolated DATA_DIR fixtures and do not touch
current/production data, cloud, deployment, restore, rollback, or Git
operations. Return a Chinese handoff directly to A00.
"""
