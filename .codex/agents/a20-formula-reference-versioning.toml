name = "A20_FormulaReferenceVersioning"
description = "Temporary Agent for immutable formula revisions and per-article keep, upgrade, or clone decisions."
sandbox_mode = "workspace-write"
nickname_candidates = ["Formula Versioning", "Reference Decisions", "公式引用版本"]
developer_instructions = """
Implement only S14 formula reference versioning after A14 and A19 are accepted.
Read REQ-20260726-004, the A00 dispatch manifest, both predecessor handoffs, and
the A20 brief. A LaTeX change or archive creates one CMS-only pending decision
per article while public rendering stays pinned to the old immutable version.
Metadata-only edits must not create decisions. Support keep old, adopt newest,
and create-and-rebind-new-card independently per article. Never bulk-apply an
Owner choice. Use isolated DATA_DIR tests. Do not implement derivation graph,
focus mode, carousel, cloud/deployment, current data, or Git operations.
"""
