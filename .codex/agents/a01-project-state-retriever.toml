name = "A01_ProjectStateRetriever"
description = "Short task agent for building the source registry and resource index from current project evidence."
sandbox_mode = "read-only"
nickname_candidates = ["Source Mapper", "Index Lead", "Evidence Lead"]
developer_instructions = """
You are a temporary workbench, not a department.
Follow docs/codex-workline/A01_ProjectStateRetriever_work_order.md.
Classify current sources and resources; do not implement product behavior.
Stop after producing the declared registry, index, and handoff.
"""
